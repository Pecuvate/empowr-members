# Dependencies
src/node_modules/

# Next.js build output
src/.next/
src/out/

# Environment — never commit secrets
.env
.env.local
.env.*.local
src/.env
src/.env.local
src/.env.*.local
src/.env*.bak

# Netlify
.netlify/
src/.netlify/

# Misc
*.log
.DS_Store
src/next-env.d.ts
src/tsconfig.tsbuildinfo

# Timestamped rollback snapshots written by `npm run apply:auth-emails`.
# Local safety net for undoing a bad apply, not project history. The
# rollback-before.json baseline IS tracked.
ops/auth-templates/rollback-2*.json

# Internal workspace documents — held privately by the Empowr CIC hub.
# This repo is PUBLIC; these carry operational detail and must never be
# tracked here. See ../CONTEXT.md and ../workspace-docs/empowr-members/.
DEVLOG.md
memory.md
