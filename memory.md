# Memory — Empowr Members

## Current Status

- **✅ 2026-09-02: EARLY BIRD IS A REAL, PURCHASABLE TICKET TIER — it was display-only copy before.** `early_bird_price_pence` existed but `mem_hold_bookings()` never consulted it: every online booking priced `case when p_walk_in then walk_in_price_pence else price_pence end`. Roller Skate Events advertised "Early bird £10" while everyone paid £15, and no allocation concept existed anywhere in the schema. Migration `20260902215853` adds `mem_occurrences.early_bird_capacity` (**per occurrence** — each event carries its own allowance; NULL = not offered) and `mem_bookings.early_bird` (a column, not derived from `price_paid_pence`, which is a snapshot that drifts from the offering's current price). Commits `6af51a2` (app) / `6aae475` (schema of record).
  - **⚠️ `mem_hold_bookings()` was DROPPED and recreated, not `CREATE OR REPLACE`d.** Adding a parameter makes an **overload** in Postgres, and two overloads leave PostgREST unable to resolve the call; the DROP then destroys the grants, which are `service_role`-only here and **not** SECURITY DEFINER. Restored explicitly and re-verified. See [[feedback_postgres_function_param_creates_overload]] before touching this function again.
  - **The allocation check sits inside the same `for update of o` lock as capacity**, after the expired-hold sweep. Each tier resolves its own price and **refuses** rather than falling back — charging £15 after someone chose £10 is worse than a visible error.
  - **Availability reads through a NEW `mem_public_early_bird()`**, not a widened `mem_public_occurrence_capacity()` — widening a `RETURNS TABLE` needs a drop/recreate of a function the live catalogue calls on every render.
  - **🔴 NO HUMAN HAS EVER BOUGHT ONE.** Verified only at the DB layer (rolled-back transaction: 10 succeed, 11th refused, £10/£15 correct, group-of-3 refused into the last ticket leaving exactly 9 rows, refused on courses/door/no-allocation). **The Stripe leg is unwalked** — nobody has chosen Early bird and been charged £10 at Checkout.
- **✅ Roller Skate Events 15+ is capacity 80 with 10 early bird tickets.** It was silently inheriting **Nunhead Sports Ground's venue default of 100** — occurrence capacity was NULL and resolves `coalesce(o.capacity, v.default_capacity)`. Now an occurrence-level override, so no other session is affected. £10 early bird / £15 standard / £20 door.
- **✅ COURSE RUNS HAVE A HEAD-COUNT AND A REGISTER — neither existed before.** `CourseRunsManager` showed `capacity 16` and nothing else (the ceiling, never the fill), and there was **no register route for a course at all** — not removed, never built, because a `per_run` course has zero `mem_occurrences` (Beginners Foundation: 14 runs, 0 occurrences) so there is no date to check into. Now `👥 N enrolled / N capacity` plus `/admin/registers/run/[runId]`, an **enrolment roll** (name, waiver, medical notes) with no check-in or walk-in panel. Its capacity reads `r.capacity` with **no venue fallback**, mirroring the course-run branch of `mem_hold_bookings()`.
  - **No paid/subscribed split on course runs, deliberately** — courses have no Subscription option (Q1, KB 2026-08-26) and `reconcileMemberBookings()` only writes `occurrence_id` rows, so `subscribed` is structurally always 0 there.
- **🔴 2026-09-02: THE ADMIN BOOKING COUNTS HAD NO STATUS FILTER AT ALL.** `bookings:mem_bookings(count)` counted cancelled, credited and refunded as booked — on the offerings screen **and** the dashboard and check-in pages, the two staff stand in front of. One cancelled booking was inflating a live number. `LIVE_BOOKING_STATUSES` now lives in `lib/business-rules` and is shared, and `lib/booking-tally.ts` holds the pure helpers (**separate from `admin-data.ts` because that carries `server-only`**, which would follow them into the client bundle). `materialize-member-bookings.ts` had documented itself as mirroring "lib/admin-data.ts's" definition — false the entire time it stood.
  - **The delete gate is `hasHistory()`, not the live count.** `mem_bookings`' FKs carry no `ON DELETE`, so Postgres refuses the delete while any row points at the occurrence, cancelled included. Gating on the live count offers a Remove button that always fails on a foreign key.
- **⚠️ A "production outage" I reported on 2026-09-02 was not real.** I grepped live HTML for `"places left"`, got zero across all 9 session pages, and reported the public capacity counter as silently broken site-wide. React SSR splits adjacent JSX expressions with `<!-- -->` — the real markup is `16<!-- --> <!-- -->places<!-- --> left`. The feature had worked since it shipped. See [[feedback_react_ssr_comment_separators_break_grep]].

- **✅ 2026-09-03: AUTH EMAIL LINKS ARE HARDENED AGAINST MAIL SCANNERS AND LIVE.** The reported invalid-link incident was not Supabase capacity: live Auth logs showed successful signup/verification followed by repeat use of the already-spent OTP, with no 429/5xx failures. Token-bearing email links now GET `/auth/confirm/start` only to stage the token in a one-hour HttpOnly cookie, redirect to clean `/auth/confirm?staged=1`, and require the member's explicit POST to `/auth/confirm/complete` before `verifyOtp()` consumes it. The `?staged=1` marker must remain — Netlify preserves the source query when the destination has none, which otherwise leaks `token_hash` back into the address bar. All six live Supabase templates are byte-identical to the repository and the redirect allow-list passes. Regression suite: `npm run verify:auth-links` (5/5). PRs #20/#22, production commits `c690bbb`/`ba4798b`.

- **✅ 2026-09-02 (sessions 5–9): PHASE 2 STEP 4 IS BUILT, DEPLOYED AND LIVE.** A subscriber's reserved place is now a real `mem_bookings` row (`source='member'`, £0, `confirmed`) instead of a live-read-only fallback — which is what finally makes **capacity** and **door check-in** see them at all. Commits `810db6e`, `bd15ca8`, `ed9ed58`, `e8b1e09`, all deployed `ready`.
  - **`lib/materialize-member-bookings.ts` is a RECONCILIATION keyed on the PARTICIPANT, not a delta off the membership that changed.** A participant can hold two Subscriptions (Kidz Monday *and* Wednesday), so "cancel this membership's rows" is not computable from one membership. Idempotent by construction — a webhook retry or a missed cron tick just re-converges.
  - Fired two ways: **synchronously from the Stripe webhook** on any membership change, and **nightly at 03:15 UTC** by `netlify/functions/materialize-member-bookings.ts` — the project's first Netlify scheduled function, and the only thing that catches an occurrence added to a slot *after* someone subscribed.
  - **⚠️ THE MODULE MUST NEVER REGAIN `import "server-only"`, AND ITS CLIENT MUST STAY INJECTED.** That guard is a module that *throws* outside a react-server bundle; it would kill the nightly function on its first line while `tsc`, `next build` and the Netlify deploy all stayed green. `npm run verify:scheduled` walks the real import graph and fails if anything reachable regains it. See [[feedback_server_only_throws_outside_next]].
  - **⚠️ `occupied` and `ownedFuture` are two different sets — keep them separate.** Creation is blocked by ANY live booking of any source **including `attended`**, because the partial unique index only covers `pending_payment`/`confirmed` and `/admin/checkin` has **no time guard** — so an early check-in would otherwise let a second row be inserted, duplicating the person on the register and against capacity. Cancellation only ever touches `source='member' AND status='confirmed'`, with `.eq("source","member")` on the update so it can never reach a paid booking.
  - **🔴 THE NIGHTLY FUNCTION HAS NEVER RUN.** First fire is 03:15 UTC. **Check its Netlify function log before trusting the safety net.**
- **✅ STAFF ALERTS ARE BUILT — this resolves the "nobody is alerted" item below.** One email per new paid **online** booking (`sendBookingConfirmationForSession`) and one per new **Subscription** (`sendStaffSubscriptionAlert`), both to `bookings@empowrcic.org`, confirmed with Empowr as an existing monitored inbox.
  - **⚠️ Deviation from the queued plan: NO `BOOKINGS_NOTIFY_EMAIL` env var was added.** The address lives in `lib/links.ts` (`staffBookingAlerts`) per this project's "never hardcode URLs in components" rule. If it ever needs to differ per Netlify context, that is the change to make — it does not today.
  - **Excluded on purpose: walk-ins** (staff created them at the door; an alert per door payment is noise on a busy session) **and Step 4 materialisation** (one subscribe creates dozens of rows). The walk-in exclusion was *documented before it was implemented* — walk-ins run the same Checkout pipeline, so the same webhook fired the same alert until `e8b1e09`.
  - The subscription alert needed its **own replay guard**: the `mem_memberships` upsert cannot report insert-vs-update, unlike the booking path's `UPDATE ... WHERE status='pending_payment'`. It checks for an existing row before the upsert, on `customer.subscription.created` only.
  - **🔴 NEITHER ALERT HAS EVER FIRED FOR REAL.**
- **✅ PUBLIC CAPACITY COUNTERS on `/sessions/[slug]`** — per date for `per_occurrence`, per run for `per_run` (a course sells as a block). **`mem_bookings` has NO anon SELECT policy**, so a direct public read returns zero rows and never errors; two SECURITY DEFINER RPCs (`mem_public_occurrence_capacity` / `mem_public_course_run_capacity`, migration `20260902153926`) expose only the two integers and count exactly what `mem_hold_bookings()` counts. **Beginners Foundation capacity set to 16** across all 14 runs.
- **✅ Home page and account page de-staled.** The home page was untouched since 2026-07-09 — still said "coming soon" six days after launch, with its primary CTA pointing *out* to EELA (which already links *into* Members to book). Now links to `/sessions`. The account page's "Keep me updated on WhatsApp" checkbox was **write-only** (nothing ever read `mem_accounts.whatsapp_opt_in` to send anything) and is now the Brevo mailing-list link; the DB column was left in place.
- **🔑 THE SESSION'S REAL LESSON — a review of the same day's work found three deployed defects.** All three passed `tsc`, `next build` *and* the Netlify deploy, because none of those can see them. **I verified what the tools could see and asserted the rest** — including telling the user, the DEVLOG and `guides/contact-routing.md` that walk-ins were excluded when they were not. The two that mattered were only provable by *running* the thing: bundling the function with esbuild, and reading the index predicate against the check-in route.

- **✅ 2026-09-02 (session 4): CANCELLED BOOKINGS WERE BEING LISTED ON THE DOOR REGISTER.** PR #18, merged `ed41e66`. `getRegister()` had **no status filter**; the register page's `confirmed`/`attended` filters feed only the **counts**, while the table renders `register.bookings` unfiltered.
  - **🔑 THE LESSON: shipping cancellation that same afternoon turned a dormant bug into a routine one.** Cancellations were rare and staff-initiated before; from then on every member who cancels would have stayed on the register they just left. **The feature and the bug were each fine alone.** Ask on any launch: *what was rare yesterday that this makes common?*
  - **Excluded, not included**: `cancelled`/`credited`/`refunded` are filtered out, everything else stays. A new status should default to **visible** on a door list — missing someone who turns up beats showing a row staff can ignore. `no_show` stays: an attendance record, not an absence.
  - **⚠️ Found only because the user asked me to delete test data.** The data was the symptom; nobody was looking for the bug.
- **🗑️ 4 test bookings deleted from the 2026-09-03 Skate Jam**, archived first to `~/.claude/backups/empowr-members/2026-09-02-skate-jam-test-bookings.json` (outside the repo — they carry a participant name). All `cancelled` with **no payment intent**; the `cs_live_*` sessions were started and abandoned, so no money moved. 0 `mem_credits`, 0 `departure_consents`; `mem_credits` is the only FK (`NO ACTION`).
  - **⚠️ `ee8e2e4a` (Skate Jam 2026-08-20, `attended`, £7) WAS DELIBERATELY KEPT — DO NOT DELETE IT.** It carries a **real live-mode Stripe payment intent**: the smoke test, and the only real card payment the platform has taken. Removing it leaves a charge in Stripe with no booking behind it — an accounting mismatch for a CIC. It is in the past and affects no upcoming register.


- **✅ 2026-09-02 (session 3): SELF-SERVE CANCELLATION IS BUILT, DEPLOYED AND LIVE.** PR #17, merged `8f9663e`. **This closes the gap session 2 opened on purpose** — Programme Policies v1.2 was published that morning promising a 48-hour member cancel window against code that still implemented v1.1's "all bookings are final". The 7 cancellable offerings are flipped to `refund_policy = 'standard'` through `PATCH /api/admin/offerings/[id]` (never SQL), and **all 9 live session pages were verified showing copy matching their own flag**.
  - **Restored from `dbbc782^` rather than rewritten**: `lib/cancellation.ts`, `POST /api/bookings/[id]/cancel`, `lib/emails/booking-cancellation.ts`, `CANCELLATION_CUTOFF_HOURS`, the cancel action in `BookingsList`.
  - **⚠️ THE ATOMIC CLAIM IS LOAD-BEARING AND MUST NOT BE "TIDIED".** Status flips to `refunded` guarded by `.eq("status","confirmed")` **before** Stripe is called, and rolls back to `confirmed` if the refund throws. Reversing that order re-opens a double-click double-refund.
  - **No credit outcome, deliberately.** The original offered refund *or* credit; redemption is Step 5 and unbuilt — nothing reads `mem_credits` — so it would issue unspendable balances. v1.2 independently says the same. Admin-issued credit on the occurrence-cancel path is untouched.
  - **A booking with no refundable payment is refused BEFORE the claim.** The webhook writes `payment_intent ?? null`, so a confirmed booking can carry none; and Step 4's £0 subscriber rows will need their subscription reservation released, which this route knows nothing about. They get the email address, not a phantom "please try again".
  - `evaluateCancellationPolicy` takes an **injectable `now`** — without it the 48h boundary is untestable. `npm run verify:cancellation` 9/9 pins 47.9h refused / **exactly 48h ALLOWED** / 48.1h allowed, plus `non_refundable` refused a month out.
  - `booking-confirmation.ts` now imports the shell from `lib/emails/shell.ts`, not `lib/email.ts` (`server-only` + Resend), so it renders outside a request — that is how both copy branches were **read back rather than assumed**. `occurrence-cancelled.ts` still uses the server-only path.
  - **🔴 NOT PROVEN: no refund has ever been taken.** No confirmed booking exists on any upcoming occurrence, so nothing on live renders a cancel button today.
  - **⚠️ ALL COPY IS DELIBERATELY SILENT ABOUT MOVING A BOOKING TO ANOTHER DATE.** v1.2 grants it; transfer is Phase C and unbuilt. Copy may lag capability, never lead it.
  - **⚠️ `Prep to Street Skate - Level 2` (inactive) is still `non_refundable`** — not on the plan's list of 7; it will contradict v1.2 the moment it is activated.

- **✅ RESOLVED 2026-09-02 (session 6/8) — BUILT, see Current Status above. Kept for the reasoning, which still governs the feature.** ~~🔔 QUEUED (agreed 2026-09-02): NOBODY AT EMPOWR IS ALERTED WHEN A BOOKING IS MADE.~~ Original note follows:
- **🔔 NOBODY AT EMPOWR IS ALERTED WHEN A BOOKING IS MADE.** Confirmed by reading every `sendEmail()` call site — there are exactly three (booking confirmation, booking cancellation, occurrence cancelled) and **all three go to the member**. `sendEmail()` sets no `bcc`/`cc`, and `ADMIN_EMAILS` is an access allowlist only, never a mail recipient. The only ways staff learn of a booking today are opening `/admin` or seeing the charge in Stripe — and **Stripe's is a payment alert, not a booking one**: no participant, session or date, and it fires identically for Heroes donations on the same shared account.
  - **Build:** a staff copy sent from the Stripe webhook, which already assembles the full `BookingEmailSummary` — so it is a second `sendEmail()` on data that exists, not a new query. New env var (`BOOKINGS_NOTIFY_EMAIL`) + Netlify config.
  - **DECIDED: per-booking, not a daily digest.** A digest delays the awareness the feature exists to provide, and volume is currently zero bookings across 142 upcoming occurrences. Revisit if drop-in volume makes it noisy — the digest is the *later* optimisation, not the starting point.
  - ⚠️ **Do not reuse the member template.** It is written to the customer ("your booking is confirmed", ticket buttons, waiver reassurance). Staff need participant name, session, date, venue and amount — and **no ticket links**, which are per-participant credentials.
  - **This is the same gap that lost every Empowr Main Site enquiry 17–25 Aug**: the write succeeded and nobody was told. See [[project_empowr_main_site]].

- **🔜 NEXT SESSION, in order (rewritten 2026-09-02 session 9 — item 2 of the old list is now BUILT):**
  1. **Check the nightly `materialize-member-bookings` function log** (first fire 03:15 UTC, 2026-09-03). It has never run. If it failed, the Step 4 safety net does not exist.
  2. **One seeded e2e that exercises the whole unproven stack at once** — a real paid booking proves the Stripe refund path *and* fires the staff booking alert *and* writes the first-ever production `departure_consents` row; a real subscription proves the webhook writes `mem_memberships`, fires the subscription alert, and materialises Step 4 rows. **Five separate "never proven in production" items collapse into two real transactions.**
  3. **Phase C: transfer** — the last unbuilt half of Programme Policies v1.2, and the reason all copy stays silent about moving a date. Plan at `~/.claude/plans/smooth-churning-rose.md`.
  4. Lower priority, still open: `Prep to Street Skate - Level 2` is inactive **and** `non_refundable`, which contradicts v1.2 the moment it is activated; and `/sessions/[slug]` still has no custom `not-found.tsx` and no `sitemap.xml`.


- **🔴 2026-09-02 (session 2): EVERY SESSION PAGE 404'd ON PRODUCTION — three fixes shipped before the right one.** **FIXED: PR #16, merged `2dee05a`. ✅ PROVEN BY A REAL ADMIN SAVE** — the team saved an offering at 12:26:15Z, which fired deploy `6a9815e7` titled *"Catalogue: offering updated: prep-to-street-skate"*, and all 9 pages stayed 200 throughout and after. **The reason string is the ordinary `offering updated` path** — exactly the edit the old gate skipped and that had been killing the site. The two previous fixes both passed every local check and both failed on the first live save. The booking funnel was dead from ~01:38 to 12:14 while `/sessions` kept listing all nine, so customers could browse and open nothing. **Nothing alerted anyone** — found by curling a booking page while checking something else.
  - **🔑 REAL CAUSE: `dynamicParams = false` is incompatible with ANY on-demand revalidation of that route.** The pages exist only because `generateStaticParams()` ran at build time; invalidating them leaves nothing able to regenerate them. Proven on production: `revalidatePath("/sessions/[slug]","page")` kills all 9; **`revalidateTag(CATALOGUE_TAG)` alone also kills all 9**; time-based `revalidate = 300` is fine; a cache-cleared rebuild restores every time.
  - **THE FIX: `revalidateCatalogue()` no longer invalidates — it REBUILDS** (`lib/revalidate.ts`), because a rebuild is the only thing that regenerates the static param set. `revalidatePath("/sessions")` stays — static route, no param set to destroy. The rebuild sits INSIDE `revalidateCatalogue` rather than at the 12 call sites, for the same reason `unwrap()` exists.
  - **`shouldRebuildForOfferingChange()` REMOVED.** It skipped rebuilds for price/copy edits — right about builds, fatal in practice, because those writes still invalidated. **The ordinary edits were the ones killing the site**; activations survived because they also triggered a build. ⚠️ Do not reintroduce that gate.
  - **⚠️ COST: every admin write now fires a Netlify build**, and an edit takes ~2 min to appear. Deliberate trade against a dead booking funnel; revisit only with evidence.
  - **(superseded) I blamed the warm `.next/cache`, then `getOfferingCached` returning `null` on error** — the one read the 09-01 "fail loud" change missed. It still returned `null` on a DB error, and the page does `if (!offering) notFound()` — so a failed read was indistinguishable from "this offering is inactive". The policy was never wrong; it was **unevenly applied**.
  - **A cached 404 IS served long past its TTL** (`Cache-Status: "Next.js"; hit` at 34,000s past expiry, even with cache-busting query strings) — but what PRODUCED it was the discarded param set, not a transient read failure. ⚠️ **PR #14 (`getOffering` throwing) did NOT stop the recurrence**; it stays merged as correct defence-in-depth, nothing more.
  - **`coverForOccurrence()`-style single-source discipline was applied here too**: the error policy now lives in `src/lib/catalogue-read.ts` (`unwrap()`), which ALL FOUR reads route through. No `server-only` guard, following `lib/age.ts`, so it is testable outside Next. **Never let a catalogue read handle its own error.**
  - **`verify:catalogue` (5/5) pins it, and the tests that matter are STRUCTURAL** — every query routes through `unwrap`, and no `if (error)` block returns instead of throwing. The behavioural tests would NOT have caught this. Both structural tests were **proven to fail** by reintroducing the bug (`4 queries but 3 unwrap() calls`).
  - **🔴 THE LESSON: I declared it fixed twice from evidence that merely FIT, and was wrong both times.** What produced knowledge each time was a **live experiment with a predicted outcome** — one admin save by the team. **Run that before calling a production fix verified.**
  - **Diagnostic lesson: the blast radius named the culprit.** `/sessions` survived because it reads the function that throws. When one page family dies and a neighbour sharing the data source lives, **diff their error policies first**. Two earlier hypotheses (warm `.next/cache`, `revalidateCatalogue` not invalidating routes) were both disproved by evidence.
  - ⚠️ **Admin edits re-trigger the render that can poison.** While this was unfixed, every team save through the admin UI was another chance to re-404 the site.
  - **⚠️ NOT failures:** the overnight `error` deploys are Netlify's "Canceled build due to no content change" — docs-only commits outside the `base` dir.

- **📜 2026-09-02: TERMS & CONDITIONS v1.2 + PROGRAMME POLICIES v1.2 ARE PUBLISHED AND LIVE** (Phase A of the Programme Policies v1.2 plan, `~/.claude/plans/smooth-churning-rose.md`). Self-serve cancel + date transfer within 48h, refund to original card; courses cancellable not movable; Roller Quad Camp and All Ages Roller Disco stay strictly non-refundable/non-transferable; no credit option (redemption unbuilt).
  - **⚠️ THE POLICY NOW PROMISES WHAT THE CODE CANNOT DO.** Booking cancellation and transfer are **published but unbuilt** — a member reading the terms will not find the controls. Deliberate, user-approved, policy-first ordering. **Phases B and C are now time-sensitive.** Membership cancellation is the exception: it already works via the Stripe Customer Portal (`bpc_1U8zvc…`, `subscription_cancel.enabled: true`, `mode: at_period_end`, `proration_behavior: none` — verified live).
  - **⚠️ PHASE D IS UNTOUCHED AND NOW CONTRADICTS THE PUBLISHED POLICY** on live surfaces: `PolicyNotice.tsx` (every booking page), `Empowr Main Site` FAQs, 4 EELA pages, `CONTEXT.md`. **The DB still enforces the old policy** — all 10 offerings are `non_refundable`, `transferable=false`. Correct count for Phase B/C is **7 refundable, 5 transferable** (NOT 7/7 as the plan said — Beginners Foundation and Prep to Street Skate are `per_run`).
  - `booking-confirmation.ts` needs nothing — its policy paragraph was already removed with a comment saying not to reinstate until self-serve exists.
  - **Sanity traps found:** `import-policies.ts` imports EVERY policy and would have **reverted four live legal documents** (org+heroes privacy/cookie are v1.2 in Sanity, v1.0 on disk) — use `import-policies-scoped.mjs`, and run `list-policies.mjs` first. The import sets only `body`; LegalHub renders Version/Last-updated from Sanity **document fields** set by `patch-policy-metadata.ts` — run both.


- **✅ 2026-09-02: A SUBSCRIBER CAN NO LONGER BE CHARGED FOR A PLACE THEY ALREADY HOLD** (`1f0f3a8`, deploy `6a97687`, live and **verified on production** — 11 checks against `members.empowrcic.org`, after 12 locally).
  - **`coverForOccurrence()` in `lib/membership.ts` is THE ONE PLACE** entitlement is resolved. The register was moved onto it. **Never add a second entitlement read** — if the two drifted, someone would be charged twice AND appear twice at check-in.
  - **The three surfaces differ on purpose:** `/api/bookings` BLOCKS (409 `already_covered`) and fails **closed**; the booking page merely disables (presentation only, degrades to not-covered); the **DOOR WARNS AND DOES NOT BLOCK**, matching the waiver status beside it — a read failure must never strand staff at a door with a queue.
  - `past_due` does NOT cover: entitlements pause, the member reverts to paying per session. Occurrences only (courses have no Subscription, Q1).
  - **This is NOT Step 4.** A covered member still cannot book at £0 — Step 4 replaces the refusal with a £0 row. `planning/phases/phase-2/CONTEXT.md` now says so, and warns against rebuilding the refusal.
  - **No subscription was needed to test it** — the guard only reads `mem_memberships`, so a directly-inserted row proves it. ⚠️ Auto mode BLOCKS the production run until switched off.

- **🔧 SMALL JOB, OPEN (raised 2026-09-02): there is NO custom 404 page.** No `not-found.tsx` exists anywhere in `src/app`, so a live site taking real card payments serves Next.js's stock black-and-white *"404 | This page could not be found."* Build one with the brand shell plus links to `/sessions` and contact. Deliberately kept OUT of the v1.2 policy job — unrelated scope.
  - ⚠️ **Do NOT turn the 404 into a "coming soon" page.** Considered and rejected 2026-09-02. It would tell typos, dead links and bots that a session is "coming very soon"; it re-creates the **soft-404** fixed on 08-28 via `dynamicParams = false` (Google treats 200-with-not-found-content as a soft 404); and a signup there carries no signal about *which* offering the person wants.
  - **The real fix for a pending offering is activation, not the 404** — `DatesComingSoon` renders inline on `/sessions/[slug]` when an ACTIVE offering has no dates. ✅ Done for Roller Quad Camp and All Ages Roller Disco on 2026-09-02; both now 200 with the coming-soon state instead of 404ing.
  - A "get session updates" line on the 404 is fine, framed as general updates. **✅ Brevo is now wired 2026-09-02** — `links.mailingList` points at its hosted form and `DatesComingSoon` links there. ⚠️ Never add an email input of OUR OWN: the hosted form is safe precisely because Brevo owns the field, the storage and the double opt-in. See the note on `DatesComingSoon` in `sessions/[slug]/page.tsx` and the EELA `/members` bug that discarded every waitlist submission.

- **📋 2026-09-02: PROGRAMME POLICIES v1.2 PLANNED IN FULL, NOTHING BUILT** — plan at `~/.claude/plans/smooth-churning-rose.md`. Scope confirmed as **all three**: booking cancel + booking transfer + membership cancel. Executes the 2026-08-18 reversal decision, decided and unacted for two weeks.
  - **🔑 Membership cancellation ALREADY WORKS** natively via the Stripe Customer Portal, already wired here. Zero code — only Programme Policies §7 forbids it. Stripe's docs also confirm one-off booking refunds can NEVER be self-served in the portal.
  - **Booking cancel is a RESTORE** — 297 lines still retrievable at `dbbc782^`. **Transfer is the only real build** and the only migration (`mem_transfer_booking()` RPC; repointing `occurrence_id` in app code bypasses the capacity lock).
  - **⚠️ A transfer must VOID the old departure consent** (`session_date`-keyed) or it fabricates a parent's answer.
  - **⚠️ POLICY SHIPS BEFORE CODE:** Sanity ×2 → LegalHub → KB → `/sync-kb`. Out of order, the CRM widget quotes the old policy to real customers.
  - All 10 offerings are currently `non_refundable` **and** `transferable=false`; 7 need flipping via the **admin UI, never SQL**.


- **🔴 2026-09-01 (later): SUBSCRIPTIONS WERE ON SALE AGAINST A STRIPE ENDPOINT THAT COULD NOT FULFIL THEM — supersedes the entry below.**
  - **The live webhook carried only `checkout.session.completed/.expired`.** The ONLY write to `mem_memberships` in the app is the upsert in the `customer.subscription.created/updated/deleted` branch; `/api/memberships/subscribe` deliberately writes nothing. So a live subscription would have **charged the card and written no membership row** — no entitlement, invisible on the register, billed monthly — and the "cancellation removes them with no admin action" behaviour below **could never have fired**. Zero live subscriptions ever existed, so nobody was affected. Endpoint fixed (3 subscription events added, verified on a **fresh GET**). `invoice.payment_failed` deliberately NOT added: unimplemented, and `past_due` arrives via `customer.subscription.updated`.
  - **Phase 2 Step 3's "DONE and verified e2e" was false and is corrected** in `planning/phases/phase-2/CONTEXT.md`. It was proven with self-signed events against a local server because the test endpoint had been dead since go-live — the lifecycle had never crossed a real Stripe delivery in *either* mode.
  - **🔑 NEXT: ONE REAL SUBSCRIPTION end to end.** Proves the webhook writes a `mem_memberships` row, and cancelling proves the register self-clears (asserted for weeks, never observed).
    - **⚠️ CORRECTED 2026-09-02 — this said it settled FOUR things and it settles TWO.** It does NOT settle the `departure_consents` write: `recordDepartureConsents()` is called ONLY from `/api/bookings` and the walk-in route (grep it), and a subscription touches neither — `/api/memberships/subscribe` writes nothing at all. That write still needs a real BOOKING, which is a separate errand. The over-capacity banner is also not settled by one subscriber unless the session's capacity is 1.
    - **Test mode can prove the webhook, but it is not the cheap option and it does not prove the live path.** The test endpoint `we_1TraTS…` is DISABLED and points at `members.empowrcic.org` — production — holding the LIVE endpoint's signing secret, so it could never verify. Restoring it needs a branch-deploy URL AND that context's `STRIPE_WEBHOOK_SECRET`. Even then `enabled_events` is PER ENDPOINT, so a test run exercises a different endpoint object than the one that was broken. **And previews share the PRODUCTION database**, so a test subscription writes a real `mem_memberships` row that entitles someone at a real door. A real £25 subscription, cancelled and refunded, is the cheaper and more honest proof.
  - **Capacity: two oversell paths closed.** The register now shows expected-vs-capacity and warns when over (`lib/register-summary.ts`, `verify:register`). Separately `mem_hold_bookings()` excluded `attended`, so checking people in **freed places** — migration `20260901192256` applied; the DB now agrees with `lib/admin-data.ts`, which already counted pending/confirmed/attended.
  - **Catalogue reads now THROW instead of returning `[]`.** A DB blip during a build shipped a **green deploy with every session page 404ing** (reproduced: exit 0, no slugs). Applies to `listActiveOfferings`, `listScheduledOccurrences`, `listCourseRuns`.
  - **Q8 is UNIMPLEMENTED and due 25 March 2027, not 3 September** (Sept 3 is the season's first session). Nothing in the schema holds a season, so no pause can be scheduled; `lib/plan-seasons.ts` only DISCLOSES it at point of sale and **should be deleted when the season becomes a plan column**. Separately, all four *year-round* plans have no occurrences past late March 2027 either.
  - **Staff guide at `/admin/guides/check-in`.** Subscribers still cannot be checked in — no booking row to mark — so no attendance is recorded for them until Step 4. Register copy corrected: it used to say "check them in as normal" when no control exists.
  - **✅ Roller Quad Camp + All Ages Roller Disco ACTIVATED 2026-09-02** via the production admin UI (deploy `6a977d5`); both render `DatesComingSoon`, verified live. **`prep-to-street-skate-level-2` remains the only inactive offering** — ADMIN UI, never SQL.

- **💳 2026-09-01: SUBSCRIPTIONS ARE ON SALE AND SUBSCRIBERS SHOW ON THE REGISTER — merged `40d2dd8`, live.** Phase 2 Steps 4-6 shipped in the form the team can launch behind.
  - **The subscribe choice lives on `/sessions/[slug]`**, beside the per-session price. ⚠️ A standalone `/membership` shop was built first and **rejected by the user** — the decision belongs where both prices are visible together. `/membership/[planId]` completes it; `/membership` is management only.
  - **🔑 THE REGISTER READS `mem_memberships` LIVE** via `plansForOccurrence()`. This is what makes launching without Step 4 viable: a Subscription reserves a place with **no booking row** (Q5), so a subscriber was invisible at check-in. Reading status live means **a cancellation removes them with no admin action** — that was the whole pain of the manual register. Waiver status via `checkWaivers()`, and this is the **only** place an unsigned waiver surfaces for a subscriber, since they never touch the booking flow. Only `active` entitles; anyone holding a real booking row is omitted rather than listed twice.
  - **All 5 plans `active=true`**, after verifying all 5 **live** Stripe Prices resolve by `lookup_key` (£25/£30/£30/£40/£45, all monthly).
  - **Age gate added to the subscribe route** — it had none, so an adult could have held a Sk8 Skool for Kidz plan and first been refused at the door a month in. ⚠️ **Kidz is 5-12, not 5-15.** All Ages 5+, Skate Jam and SYNKRON8 15+. Pure helper in `lib/age.ts` (not `membership.ts`, which is `server-only` and untestable outside Next); `verify:subscriptions` 25/25.
  - **🔑 Magic-link sign-in broke on the first Netlify preview.** `magic_link` is the **only** template built from `{{ .RedirectTo }}`; Supabase silently substitutes the bare `site_url` when the origin is not allow-listed, producing `members.empowrcic.org&token_hash=…` with **no `?`** — an invalid URL the browser refuses. Fixed by allow-listing `https://*--empowr-members.netlify.app/**`, **not** by switching to `{{ .SiteURL }}` (that drops the per-request `?next=` and kills previews entirely). `npm run check:auth-emails` now asserts the allow list and was **proven to fail** on a missing origin.
  - **Catalogue layout**: dates paged 6 at a time (query raised 30→200 so "Later" reaches the end of what is scheduled), year restored to `formatOccurrence` (the shared formatter — emails and tickets dropped it too), venue on **every** row resolving occurrence-then-offering (Kidz Mondays inherit Goldsmiths and rendered blank), subscribe moved to a sticky sidebar card, `CourseRunList` given the same shell as `OccurrenceList`, and `/sessions`' Clear button always mounted so selecting a filter no longer reflows the grid.
  - **🔴 I put production into the listed-but-404 state for ~6 minutes** by activating Roller Quad Camp and All Ages Roller Disco in **raw SQL** — that fires neither `revalidateCatalogue()` nor `triggerCatalogueRebuild()`, and `/sessions/[slug]` fixes its params at build. Reverted to inactive and forced a cache-cleared rebuild. **Activate those two through the ADMIN UI after a deploy, never in SQL.** A "dates coming soon" state is built and waiting for them. **Both were activated via the admin UI on 2026-09-02 and it rendered in production for the first time**; its interim mailto became the real Brevo mailing-list link the same day (`2cf6056`). Still **deliberately no email input of ours** — a field that drops addresses is the EELA `/members` bug.
  - **✅ CLOSED 2026-09-02 — the double-pay gap is fixed** (`1f0f3a8`, live, verified on production). See the top entry. **STILL OPEN:** the `departure_consents` write has **still never executed in production** — and note it needs a real **booking**, not a subscription.

- **✅ 2026-08-31: PASSWORD RESET BUILT, SLUG RENAMED, AND PHASE 2 UNBLOCKED — Steps 4-6 are now the next build.**
  - **Password reset exists** (`5b21b85`): `/login` → "Forgotten your password?" → emailed link → `/account/password`. ⚠️ The request form **always reports success even for an unknown address** — otherwise it is a way to test whether someone holds an Empowr Members account, and these belong to parents of children at known sessions at known times. `/account/password` is an ordinary guarded member page, not a special reset route: the recovery link runs `verifyOtp` in `/auth/callback`, so the member already holds a session by the time they arrive and that session IS the authorisation. **Proved e2e, including completing the reset in a different browser from the one that requested it.**
  - **⚠️ Middleware was SWALLOWING auth errors** (`53819c4`) — reported by the user as "it signed me into the wrong account". **Nothing crossed accounts**: `verifyOtp` resolves a token to the user it was issued for or fails, and this one had been consumed, so no session change happened. But `url.search = ""` deleted `?error=` when bouncing a signed-in visitor off `/login`, so a dead reset link landed on `/account` with **no message at all — reading as success**. A member would believe their password changed when it had not. Same swallow hit expired magic links.
  - **`beginners-foundations` → `beginners-foundation`** (`e4b6b83`), 308 on the old URL, verified live. Nothing linked to it; **EELA's `/adults/sk8-skool/beginners-foundations` is its own page in a separate project and is still plural.**
  - **⚠️ A SLUG RENAME CAN SILENTLY NOT TAKE EFFECT ON A REBUILD.** `generateStaticParams` reads `listOfferings()`, wrapped in `unstable_cache`, and **Next persists that cache in `.next/cache` between builds — Netlify restores it too.** The first build after the rename still emitted the old slug; only `rm -rf .next/cache` fixed it. **This is a hole in what `lib/rebuild.ts` guarantees** — it fires to add a new slug to the static params, but a build restoring a warm catalogue cache can fire and change nothing, producing the listed-but-404 page it exists to prevent. **Deploy with `clear_cache: true` after any write that changes the active slug set.**
  - **✅ PHASE 2 Q3/Q5/Q6/Q8 ALL CLOSED** (`a3439aa`, `fb84ab2`) — intake doc closed out, one ADR row each. **No cap** (`sessions_per_period` NULL); **place reserved indefinitely, no re-booking ever**; **warn then revert to pay-as-you-go**; **Skate Jam pauses and auto-resumes** via `pause_collection` + `resumes_at`.
  - **⚠️ Q3 came back as "4 a month" and was pushed back on, not recorded.** The 52÷12=4.33 reasoning was right, but a cap of 4 charges for 4.33 and delivers 4 in the ~4 months a year with five occurrences — **and it contradicts Q5**: a place held indefinitely is held all five times, so a cap means refusing a child at the door from a place they hold. Alternative offered (cap at 4, price ~£27.70) not taken.
  - **🔴 Q5 IS THE BIGGEST ITEM IN STEPS 4-6 AND IS NOT A CONFIG FLAG.** Capacity, the waiver gate and the door register **all key off a booking row**, so the platform must CREATE those rows on the subscriber's behalf for every occurrence in their slot — handling a lapsed waiver, a full session, and the seasonal pause.
  - **Stripe dunning VERIFIED in the Dashboard** (no API exposes it): cancel after all retries, invoice **left past-due**, **4 attempts over 2 weeks** (Stripe recommends 8). ⚠️ **The cut from 8 to 4 is correct ONLY because failed-payment emails are being enabled** — retries are silent otherwise, so fewer would trade recovery for nothing. **Never change one of those two settings without the other.** All account-level, shared with Heroes. ⚠️ "Leave past-due" is an inherited default, not a decision — it leaves an uncollectable receivable open forever; *mark uncollectible* is tidier for Xero.
  - **Q6 notification split, not duplicated:** Stripe's own failed-payment email owns the per-attempt nudge (its copy **cannot** be edited, branding only); Members builds ONE branded email for the terminal event — the reserved place is gone, and how to resubscribe.
  - **KB updated and `/sync-kb` run** — customer-facing terms in `entities/sessions`, machinery in `sessions-internal`, widget index rebuilt and deployed.

- **🔑 2026-08-30: AUTH EMAIL LINKS NO LONGER BREAK IN A SECOND BROWSER** (`ccadb6d`, applied live + pushed). Every template used `{{ .ConfirmationURL }}`, which under Supabase's default PKCE flow is redeemable **only by the browser that started the flow**. Anywhere else it returned *"That sign-in link is invalid or has expired"* — untrue, and unactionable for the member.
  - **⚠️ NOT just a device-switch bug — a different browser on the SAME device fails too.** The likely victim is someone signing up in Safari and tapping the link in the Gmail app's in-app browser. It landed on the **signup confirmation, the first email any member receives**.
  - **✅ PROVEN BOTH WAYS against production.** Before: signup in profile A, confirm in profile B → bounced to `/login`. After: same sequence → landed on `/account` signed in. Not inferred.
  - **Fix is Supabase's own documented pattern**, not a workaround: send `{{ .TokenHash }}`, verify with `verifyOtp()`, which is not browser-bound. **`/auth/callback` ALREADY had that branch** — only the emails pointed at the PKCE one. `type` values taken from the `EmailOtpType` union in installed `@supabase/auth-js`, because the docs are ambiguous.
  - **⚠️ magic_link uses `{{ .RedirectTo }}`; signup deliberately does NOT.** Magic link's destination varies (`/login?next=/book/x` must return to the booking page) and LoginForm always sends `?next=`. Signup always lands on `/account`, so coupling it to RedirectTo buys nothing — **and I proved the cost**: applying the RedirectTo version first produced `/auth/callback&token_hash=...` with no `?`, because the template shipped ahead of the form change it depended on. Live for ~1 min. **Same ordering hazard as the logo asset, from the other side: never apply a template that depends on undeployed code.** Now designed out, not documented.
  - Known trade-off accepted: `{{ .SiteURL }}` is fixed to production, so **a signup on a deploy preview now confirms against production**. Magic link still follows RedirectTo, so previews keep working there.
  - **✅ `npm run apply:auth-emails` NOW EXISTS** — the missing piece behind 08-29's hand-written PATCH. Sends `payload.json` byte for byte, snapshots live config to a gitignored `rollback-<stamp>.json` first, **refuses any payload still containing `ConfirmationURL`**, and verifies against a **fresh GET, never the PATCH response body** ([[feedback_verify_claims_about_own_code]]). Token resolution shared via `management-token.mjs` instead of copied. `check:auth-emails`: 6 in sync, 0 drifted.
  - **⚠️ Auto mode blocked the apply twice** — expect it on every auth-config change, see [[feedback_automode_blocks_production_writes]].

- **✅ 2026-08-30: DOOR + ONLINE DEPARTURE CONSENT UI VERIFIED IN A BROWSER — but the WRITE is still unproven.** Throwaway account + age-10 participant, driven through the deployed `/book` and a local admin server (`ADMIN_EMAILS` overridden so prod env stayed untouched). All rows deleted after, every table back to a pre-test baseline.
  - **Both surfaces gate identically**: consent off → bookable; on with an empty checklist → blocked + notice; all five ticked → bookable; `other` with no description → blocked; described → bookable. Zero page errors, so the `BookingForm` refactor did not break the client bundle.
  - **The "never pre-consents" rule works live** — with no `default_travel_method` on file the block starts collapsed and every confirmation starts unchecked, on both surfaces. `0cf6be8` confirmed too: the missing-waiver warning shows in red while **Take payment stays enabled**.
  - **🔴 STILL NOT PROVEN: that a `departure_consents` row actually lands, with the right `session_date`.** I exercised the gate, not the write. Needs one real charge. **Narrow the red flag to the write — the UI is done.**
  - **⚠️ Before this, the only participant in the whole DB was a 38-year-old**, so the consent block had never rendered against real data on *either* surface, and every `default_travel_method` was null so the pre-fill branch had never run. The 08-29 note framed this as a door-only gap; it was both.

- **🚪 2026-08-29: DEPARTURE CONSENT IS NOW CAPTURED AT THE DOOR, and waiver status shows in walk-in search.** Merged to `main`, deploy `5533eb9` **ready**, live routes verified (public 200, unknown slug still 404, `/bookings` + `/admin` still 307).
  - **The door now asks the same departure question as the online flow** — same fields, same completeness rule, same `departure_consents` table, same `session_date`, so a door consent appears in the Waivers staff portal exactly like an online one. Previously captured NOWHERE; the panel just told staff to collect it on paper, at the one surface where a child is most likely to be leaving imminently.
  - **Still OPTIONAL, exactly as online** — default is collected-in-person, block starts collapsed, checklist starts unchecked. **⚠️ Deliberately NOT pre-filled from `default_travel_method`**: the travel *method* pre-fills, the consent never does. Pre-consenting would fabricate a parent's answer, which is the whole reason it is per-booking (2026-08-10). Pinned by a test.
  - **⚠️ Waiver status in search WARNS, it does not BLOCK — and I shipped the contradiction first.** I documented it advisory-with-route-authoritative, then hard-disabled the button on it (fixed `0cf6be8`). It resolves once at search time and fails to "unsigned" if an account email lookup errors, so blocking means one transient failure strands staff at a door with no override.
  - **⚠️ Search calls `checkWaivers()` — the SAME function the route gates on, never a copy.** The old code omitted waiver status deliberately to avoid a second gate that drifts; that reasoning is preserved by reusing the function. **Do NOT swap it for a direct `mem_waiver_consents` lookup for speed** — that reintroduces the copy AND silently misses everyone covered only by the legacy fallback (standalone waiver-app signers).
  - **New shared modules, because this app has shipped the duplicate-component bug three times**: `lib/travel-methods` (canonical values), `lib/departure-consent-form` (state/defaults/completeness), `components/booking/DepartureConsentFields`. `BookingForm` now uses all three.
  - **⚠️ `travel-methods` is SEPARATE from `validation` for a measured reason — do not merge them.** `validation.ts` builds zod schemas at module scope, so a **value** import from a client component pulls zod into the bundle: routing the shared module through it cost **21 kB First Load JS on both `/book` routes (140 kB vs 119 kB)**, the paid booking path. Type-only imports from `validation` are free and still used.
  - **`npm run verify:departure-consent` (6/6)** covers the client-builder → server-schema seam, in both directions. The failure it guards: booking succeeds, card charged, safeguarding record dropped by a 400 nobody reads.
  - **🟠 PARTLY VERIFIED 2026-08-30 — see the top entry.** The UI and the gate are now exercised in a browser on both surfaces, and `/book` was clicked through after the refactor. **Still unproven: the `departure_consents` write itself.** No walk-in has been taken through against a real session. Build clean + tests green ≠ used. See [[feedback_deployed_not_verified]].

- **✉️ 2026-08-29: auth-email logo CENTRED — email only.** `text-align:center` on the header cell **plus `margin:0 auto` on the img** (it is `display:block` for Outlook, so `text-align` alone will not do it). **`SiteHeader`/`AdminHeader` stay LEFT-aligned** — the user confirmed email-only after I first read "move the logo centre" as brand-wide across all 8 Empowr properties.
  - **🔴 I DESYNCED ALL SIX LIVE TEMPLATES BY HAND-WRITING THE PATCH.** `render:auth-emails` emits `ops/auth-templates/payload.json` **so nobody hand-writes it**; I wrote the body from source and dropped the 17-line header comment. All six live templates then differed from the repo. **`check:auth-emails` would have said 6 drifted and I could not run it, so it said nothing.** Caught only by a manual `payload.json`-vs-live comparison during a review the user asked for. **USE payload.json.**
  - **I also reported it "verified" when it was not** — a fresh GET confirmed the *centring* landed; **byte-identity, the thing that mattered, was never checked**. Corrected in `bdbaea0`'s message since `087814b` was already pushed. Same family as last session's four wrong claims.
  - **✅ The guard is now RUNNABLE FROM A COLD SHELL** (`bdbaea0`): `check-auth-templates.mjs` resolves the token itself — env first, then workspace `.env.shared` by walking up from the script; Bearer header only, never logged. Now reports **6 in sync, 0 stock, 0 drifted**. **A check that takes a puzzle to run is a check that does not get run** — that was the actual root cause, not carelessness alone.
  - **⚠️ An `ops/`-only commit is CANCELLED by Netlify as "no content change"** (base dir is `src/`). Expected, not a failed deploy.
  - **📐 `brand-identity.md` (in `empowr-cic-workspace`, separate repo) now documents the white variant** `_brand/logos/empowr-logo-transparent.png` (4.78:1 on brand blue vs the master's 2.33:1) with a **mandatory pairing rule**: that asset is for the branded email header band and nowhere else, and contrast is never again to be solved by inventing a chip. This closes last session's flagged root cause at source.

- **✉️ 2026-08-28/29 (s2): AUTH EMAILS BRANDED — all 6 applied, verified live, MERGED to `main` (`13297e1`) and deployed.** Session ran across midnight; dates taken from the commit record.
  - **🎨 Header: use `/logo-white.png`, no chip, no text span.** `_brand/logos/empowr-logo-transparent.png` is the **official pure-white variant** (same artwork, same 107,341 opaque pixels, ink `#ffffff` — measured). The old white rounded chip was a workaround I invented for a navy logo **without checking the brand folder**. Contrast on brand blue `#4a70c2`: **white 4.78:1 vs navy 2.33:1**. Also dropped the `Empowr CIC` text span — the logo artwork already contains the wordmark, so the name rendered twice. Verified in a real inbox old-vs-new. **⚠️ The absolute logo URL means the asset must be DEPLOYED BEFORE the templates are applied**, or every email in between 404s its logo.
  - **⚠️ `brand-identity.md` documents only `_brand/logo.png` and never mentions the white variant** — that omission is the root cause of the chip. Fix at source.
  - **`npm run check:auth-emails` reports 6 in sync, 0 stock, 0 drifted** — every template `custom=true` and byte-identical to the repo output. `confirmation` and `magic_link` (the only two reachable in-app) went first; the other four landed once auto mode was off. **The classifier refused both a curl PATCH and an MCP write while auto mode was on** — expect that on any future auth-config change. See [[feedback_automode_blocks_production_writes]].
  - **⚠️ Auth on `empowr-cic` is Members-ONLY — verified, not assumed.** Zero `signInWith|signUp(|auth.getUser|createBrowserClient` hits across Waivers, EFN dashboard and EFN public site, and `site_url`/`uri_allow_list` name only `members.empowrcic.org`. Auth config is project-level, so this check is mandatory before ever touching it again on this shared project.
  - **A memory claim here was corrected, and my correction to it was the actual error.** memory.md said "unstyled defaults"; the copy looked hand-written so I recorded that it had been rewritten. Wrong — `mailer_templates_custom_contents` is authoritative and read `false` for all six. Supabase's stock copy is just better than it used to be. **The original memory was right.**
  - **The brand shell moved to `src/lib/emails/shell.ts`** (pure, no `server-only`, no Resend); `lib/email.ts` keeps Resend transport and **re-exports** it, so all three consumers are untouched. Done because Supabase templates can't import at send time, and a hand-written second copy is the `AdminHeader` failure mode. `next build` clean, routes unchanged.
  - **`ops/scripts/alias-loader.mjs` resolves `@/*` so plain node can run `src/` code.** Node 24 strips types itself; the alias was the missing piece. **Nothing under `lib/emails/` was runnable or testable before this** — reusable for any future `node --test` suite.
  - **`npm run render:auth-emails` fails loudly if a `{{ .Xxx }}` placeholder got HTML-escaped** — an escaped placeholder still renders and still looks fine, it just ships a dead link. `npm run check:auth-emails` diffs live vs repo and is **the only thing that catches `shell.ts` changing without a re-apply**, since a green build says nothing about config stored in Supabase.
  - **`recovery` is deliberately NOT worded as a password reset.** No `resetPasswordForEmail()` call and **no set-a-new-password screen exists** — `/auth/callback` just signs you in and drops you on `/account`. The stock copy pointed at a flow that does not exist. If password reset is ever built, rewrite that template in the same change.
  - **⚠️ A caveat I recorded mid-session was FALSE — do not reinstate it.** I wrote that a PATCH here normalises `security_captcha_provider`/`sms_provider`/`sessions_timebox` to `null`. **It does not** — those nulls appeared only in the MCP write's **response body**, and a fresh `GET` shows all four unchanged. **A response body is not a read.** Verified unchanged after the PATCH: `site_url`, `uri_allow_list`, `mailer_otp_exp`, `mailer_secure_email_change_enabled`, `disable_signup`, all SMTP fields. See [[feedback_verify_claims_about_own_code]].
  - **✅ VERIFIED E2E with a real production signup**, then cleaned up. Delivered email carries the preheader, logo (`/logo.png` → **HTTP 200, image/png**), wordmark, branded heading, body copy, CTA, the copy-paste fallback link, the one-hour expiry line and the footer. `{{ .ConfirmationURL }}` substituted into a real verify URL with **`redirect_to=https://members.empowrcic.org/auth/callback`** — the custom domain, i.e. the `e4aaa73` fix holding.
  - **✅ USE `tech@pecuvate.com` FOR TEST EMAIL — user's standing rule, ALL projects.** Use `tech+<tag>@pecuvate.com` when a distinct account is needed: `tech@pecuvate.com` already holds a real Members account (`b649352a`), so a bare signup collides. See [[feedback_standard_test_email]].
  - **⚠️ I recorded that `teams+` addresses do not deliver. That was WRONG — the user confirmed receipt.** The signup sent cleanly and every Gmail search came back empty, so I called it a delivery failure. The connector here reads a mailbox carrying `general@`/`enquiries@`/`finance@`/`apps@` and not `teams@` — **an empty search proves this tool cannot see that mailbox, not that mail failed.** I turned a correct note into an incorrect one. See [[feedback_absence_of_evidence_in_one_tool]].
  - **Cleanup proven against a pre-test baseline**: `auth.users` 4→6→**4**, `mem_accounts` 3→5→**3**, zero `%authbrand%` rows, remaining email list byte-identical to before. Child rows deleted before parents in one transaction.
  - `.gitattributes` pins `ops/auth-templates/**` to LF — the drift check compares bytes, so a CRLF checkout would report DRIFT on untouched files.

- **🚪 2026-08-28 (later): drop-in eligibility corrected across the catalogue, and the soft-404 fixed.**
  - **All 6 drop-in-eligible offerings now carry a door price**: Skate Jam £10, Roller Skate Events £20, **All Ages Roller Disco £20** (inactive), Sk8 Skool Kidz £10, All Ages £12.50, SYNKRON8 £15. The three lessons match their online price; open skate and events carry a premium — **Empowr's stated structure, confirmed 2026-08-28**, not an inference. Courses and the camp refuse walk-ins at the database level.
  - **Set via `PATCH /api/admin/offerings/[id]`, never raw SQL** — that route calls `revalidateCatalogue()` and the door price renders publicly. All five active ones verified rendering live.
  - **⚠️ `/sessions/[slug]` is now `dynamicParams = false`.** That is the ONLY thing making an unknown or inactive slug return a real 404: the page always called `notFound()`, but ISR **stored the not-found render as a prerendered page** and served it 200. Reproduced on a local `next start`, so it was app behaviour, not the CDN.
  - **⚠️ Consequence — the slug set is frozen at build time, and `revalidateCatalogue()` CANNOT extend it.** Revalidation re-renders existing params; it never adds new ones. `lib/rebuild.ts` therefore fires a Netlify build hook when the set of **active** slugs changes (create-active, `active` flipped, active slug renamed). **Price/copy edits deliberately do not.** Remove one and you must remove the other, or activating an offering yields a listed session that 404s.
  - Verified in production: price-only PATCH → **no** build; hook POST → real deploy. **The activation branch itself was not exercised** — it would have published a dateless session on a live site.
  - **⚠️ `NETLIFY_CATALOGUE_BUILD_HOOK` is NOT `is_secret`** — this Netlify plan refuses it (scopes are paid, 403; a secret can't take the default `all` scope, 422). Fine for a build hook; do not copy for anything granting data access.
  - **KB restructured** (`vaults/EMPOWR CIC`): the rule is "a session accepts drop-ins unless it is **sold as a block**". The "Structured Lessons" heading is a trap — all three sessions under it ARE drop-in. `sessions-internal` held Roller Disco's price in prose ("priced per event (£15) and that is the whole offer") and was missed by a field-shaped search; now amended.

- **🚪 2026-08-28: PAY-ON-THE-DOOR WALK-INS ARE LIVE** (`775316d` / merged `a2f9def`), e2e-verified through the deployed production app on live-mode Stripe. Staff take a walk-in from a session's register; the member pays the door price by QR or link on their own phone; the existing webhook confirms and emails the ticket.
  - **Migration `members_hold_bookings_walk_in_mode`** adds `p_walk_in` to `mem_hold_bookings()`. The 5-arg signature is **DROPPED, not overloaded** (a default-arg overload makes a 5-named-arg PostgREST call ambiguous). Ledger 34/34 clean.
  - **⚠️ The RPC needed a real change, contrary to the spec.** The online path requires `starts_at > now()`; walk-in mode tests **`ends_at > now()`** because a walk-in arrives *after* the session starts. Without it the feature would have refused the exact person it is for.
  - **⚠️ Door price REFUSES rather than falls back** — null `walk_in_price_pence` raises `mem_no_walk_in_price`. **Only Skate Jam (£10) and Roller Skate Events (£20) have one.** Sk8 Skool Kidz, All Ages and SYNKRON8 are NULL, so the panel visibly refuses them **until Empowr sets a door price**. This is the main open action from this work.
  - **Course runs refuse walk-ins** (`mem_walk_in_not_supported`) — the door price is a single-session price; a course is sold as a block.
  - **Deliberate limit: departure consent is NOT captured at the door.** Per-booking judgement (2026-08-10); defaulting it from `default_travel_method` would fabricate a parent's answer. The panel warns staff for under-18s. Obvious next increment.
  - New surfaces: `GET /api/admin/participants/search`, `POST /api/admin/walk-ins`, `POST /api/admin/bookings/[id]/release`, `WalkInPanel`, `ReleaseHoldButton`. `getOrCreateStripeCustomer` now takes account fields, not `AuthedAccount` (the door charges an account the caller is not).
- **🔴 2026-08-28 FIXED, was live and silent: every sign-in redirected members OFF the custom domain** (`e4aaa73`). `/auth/callback` used `new URL(request.url).origin`, which behind Netlify is the internal deploy host, so sign-in at `members.empowrcic.org` answered `307 -> main--empowr-members.netlify.app`. The session cookie is **host-only**, so the member arrived with no session and bounced to `/login` — on the magic-link sign-in AND the **signup confirmation, the first email any new member gets**. Fix: use the existing proxy-aware `requestOrigin()`, already used by every Stripe redirect. **Found by e2e-testing an unrelated feature, not by a report — nobody had noticed since launch on 08-27.**
- **⚠️ Two register gaps closed at the same time:** `/admin/checkin` hid a session the moment it started (its doc comment described the intended filter; the query underneath said `starts_at >= now()`), and a pending hold had no early release, so a room read as full for ~41 min while people queued. **Shortening the hold is NOT an option** — Stripe Checkout enforces a 30-minute minimum expiry.

- **🚀 2026-08-27: THE SITE IS PUBLIC.** `noindex` removed from the root layout, `app/robots.ts` added (`/robots.txt` previously served the app's own 404 HTML), commit `5adb680` deployed and **verified on the live site** — robots.txt 200s with real rules, `noindex` absent from static and dynamic routes, all 8 slug pages 200. **Live-mode Stripe since 08-18, so real cards now charge.**
  - **Catalogue 1 → 7 active offerings** (briefly 8; Prep to Street L1+L2 later merged into one). Skate Jam, Sk8 Skool Kidz (Mon + Wed), All Ages, SYNKRON8, Beginners Foundation, Prep to Street Skate (one offering, both levels as course runs), Roller Skate Events 15+. Roller Quad Camp and All Ages Roller Disco stay inactive — Camp has months but no dates in Empowr’s own doc, Roller Disco is not in it at all.
  - **98 occurrences seeded to late March 2027.** Christmas closure (20 Dec–3 Jan) skipped; Skate Jam ends at its 25 Mar season end; Kidz Wednesdays carry Honor Oak explicitly. Built via `AT TIME ZONE 'Europe/London'`.
  - **20 course runs** — Beginners Foundation 14 (7/level), Prep to Street 6 (3/level), all capacity 25. **`per_run` courses need NO occurrences**; and the course-run path in `mem_hold_bookings()` has **no venue fallback**, so NULL capacity there is genuinely unlimited.
  - **Anniversary event live**: new venue Nunhead Sports Ground, Sat 3 Oct 2026 + 2 Oct 2027, 19:00–22:00, **capacity 100**.
  - **⚠️ ALL offerings now `non_refundable`, which HARD-BLOCKS self-serve cancellation** — `evaluateCancellationPolicy` short-circuits on that value. Restoring the feature needs them back on `standard` first.
  - **Self-serve cancellation is a RESTORE, not a build** — `dbbc782` deleted 458 lines of working, e2e-proven code; zero migrations needed. The work is the policy chain (Sanity ×2 → KB → `/sync-kb` → code).
  - **NOT DONE:** no e2e booking against the `per_run` path since dates were set; EELA cutover unmerged (its branch predates the 4→10 catalogue change); Nunhead capacity of 100 is Empowr's number but untested against a real sell-out.

- **✅ 2026-08-27: PHASE 2 STEPS 2-3 DONE, MERGED, DEPLOYED AND VERIFIED END TO END.** Nothing is user-reachable — all **5** plans are `active=false` and no page renders one.
  - **Subscriptions are per PARTICIPANT and per WEEKLY SLOT** (Empowr, 2026-08-26). Sk8 Skool for Kidz is **£30 per slot** — Mon 16:00 and Wed 17:00 are separate plans, so a child doing both pays £60/month. The KB summary table implied one £30 plan for both days and **caused a wrong model to be built** before it was caught; corrected at source.
  - **Prices resolve by `lookup_key`, never Price ID** — one Supabase project serves every environment but production is LIVE Stripe and previews/local are TEST. `stripe_price_id` is held NULL by a CHECK constraint.
  - **⚠️ Slot matching MUST compare in Europe/London** (`lib/slot-matching.ts`). A UTC comparison shifts the hour, and for late sessions the weekday, across BST — a "Mondays 16:00" slot would match all summer and silently stop each October. Tested both sides.
  - **Members has its OWN Customer Portal config** (live `bpc_1U8zvc…`, test `bpc_1U8noL…`, plan switching off). The account default belongs to **Heroes** — editing it would silently remove donors' tier switching. Resolution fails 503 rather than falling back.
  - **Verified through the deployed app with the LIVE key**: `POST /api/memberships/portal` returned a real `portal_url`.
  - **Q6 answered: end-of-retry action is "Cancel the subscription"** — account-level, **no per-app override**, so it applies to Heroes donors too.
  - **OPEN — all with the team, and they gate Steps 4-6:** Q3 (caps — recommend none; £30/mo already prices in 4.33 sessions), Q5 (does a subscriber still reserve a place, given capacity and the door register need a booking row), **Q8 (Skate Jam is seasonal Sept 3–Mar 25 — what does a monthly subscription do out of season? blocks that plan alone)**.

- **🔑 2026-08-26: THE GOVERNING RULE — `vaults/EMPOWR CIC/entities/sessions.md` is the single source of truth for what sessions exist.** EELA displays it; this catalogue must correlate with it; anything diverging is a defect to correct **toward the KB**. **Wix is explicitly OUT OF SCOPE** as a reconciliation target — do not audit against it or re-raise it.
  - **Catalogue rebuilt 4 → 10 offerings, every new one `active=false`** (nothing public — `listOfferings`/`getOffering` both filter on `active`). New: `prep-to-street-skate-level-1` (Southwark Park) and `-level-2` (Dulwich Park), kept **separate** because their venues genuinely differ; `all-ages-roller-disco` (Ladywell, £15, 5+). `beginners-foundations` stays ONE offering with **two course runs** (L1 Tuesdays / L2 Wednesdays, £55, dates TBC) because both levels share Honor Oak. New venues Southwark Park + Dulwich Park; Honor Oak/Goldsmiths given full KB postcodes.
  - **Names are now KB-canonical**: "SYNKRON8: Roller Dance for Beginners", "Roller Skate Events 15+", and **"Beginners Foundation" SINGULAR** (Empowr: it's the foundation of a skater's skills). ⚠️ **Both slugs still read plural** (`beginners-foundations`; EELA route `/adults/sk8-skool/beginners-foundations`) — open decision, and only free to change while nothing is live.
  - **Beginner Street Skate is deliberately NOT here and must not be added.** Free (£0), and it is the Outside Skating Pathway's *destination* — the paid L1/L2 prep courses are the on-ramp (razor-and-blades, razor given away). A £0 booking wouldn't survive Stripe Checkout as built. It stays in the KB; its absence here is correct.
  - **Skate Jam is SEASONAL: Sept 3 – Mar 25 only.** One out-of-season occurrence (27 Aug, 0 bookings) was deleted; 13/20 Aug left alone — both past, and 20 Aug holds the retained live £7 booking. **Sk8 Skool Kidz Wednesday runs year-round** (Sports Field outdoors Apr–Aug, Honor Oak indoors otherwise) — 9 missing Sept–Oct Wednesdays added, DST shift handled.
  - **Open: `refund_policy`.** 4 offerings sit on `standard` (admin UI labels it "48h refund/credit window") while the KB marks every session non-refundable. **Deliberately parked** — Programme Policies v1.2 reverses this stance, so set it once in that pass.
  - **`mem_occurrence_status` has no neutral `cancelled`** — only `scheduled`/`cancelled_by_empowr`/`completed`, and `cancelled_by_empowr` carries real member entitlement. Wrong value for a seeding error — delete the row, guarded on zero bookings.
  - Supabase MCP dropped mid-session; the Management API fallback works (token from `.env.shared`) — see `[[reference_supabase_management_api_sql]]`. Auto mode blocks these prod writes until approved.

- **♿ RESOLVED 2026-08-27 (PR #12 merged, `b745c8d`): `input#age-filter` focus indicator is fixed on `main`.** ⚠️ It had been "fixed" once before, on 2026-08-24, but the commit sat on the unpushed `feat/eela-booking-cutover` branch while DEVLOG and memory both claimed it shipped — production carried the defect for three days. See [[feedback_commits_stranded_unpushed]]. Original finding follows: **2026-08-20: `input#age-filter` on `/sessions` has no visible focus indicator** at any of 8 viewports. Found by an external audit (Web Build Framework harness), read-only — no code changed. Small fix. **Also confirmed by the same run: PR #8's layout fix is intact** (the harness's initial phantom-scroll flag was a checker bug, disproved by probing the live page — 881px of genuine content in a 667px viewport), and PR #9's unified header still sets `aria-current` correctly. `(member)`/`(admin)` routes remain unaudited — the harness has no login step yet.

- **[SUPERSEDED 2026-08-27 — `noindex` REMOVED at public launch; `robots.txt` now exists. The "AT LAUNCH" action below is DONE.]** 2026-08-19: `noindex` SHIPPED, deployed and verified live (PR #10, MERGED). `robots: { index: false, follow: false }` on the root layout's `metadata`; every route inherits it because no page sets its own `robots`. Confirmed **on the live deploy** (`6a85ff97`, commit `135f91c`) on `/`, `/sessions`, `/sessions/skate-jam`, `/login`, `/signup`, `/waiver` and `/ticket/[id]` — **including the dynamic routes absent from the build output**, which had been flagged as unverified rather than assumed. No regressions: `/sessions` still `o (Static)`, `/sessions/[slug]` still SSG, zero skeleton markers, `/bookings` + `/admin` still 307 for anonymous.
  - **Deliberately NOT paired with a `robots.txt` `Disallow`** — that would stop crawlers ever *reading* the tag, and a blocked page can still be listed as a bare URL. There is a code comment saying so; **do not "complete" it by adding a Disallow.** See [[feedback_noindex_not_robots_disallow]].
  - **A password gate was assessed and DECLINED as disproportionate** — the user is the only reviewer. It needs Netlify Pro, or ~30 lines of edge/middleware basic auth; **and a blanket gate would 401 the Stripe webhook, leaving payments taken with bookings never confirmed.** Revisit only if the link is shared with other reviewers.
  - This is **release control, not security** — nothing sensitive is exposed. The site remains reachable by URL by design.
  - **AT LAUNCH:** remove the two `noindex` lines **and** add a real `robots.txt` — the site has never had one, `/robots.txt` currently serves the app's own 404 HTML.
- **2026-08-19: analytics — the site ALREADY HAS IT; do not re-instrument.** The user asked how hard it would be to add analytics "since we don't have it". **Wrong premise.** PostHog has been live since 2026-07-30; verified this session that `posthog-js` is a dependency and the library is bundled into the chunks the live site serves. **Grepping the served HTML for "posthog" returns nothing and that is expected** — it initialises client-side in a `useEffect`, so check `.next/static/chunks/` for `us.i.posthog.com` instead. The two real gaps are (1) event flow still unverified end-to-end (and Playwright *cannot* prove it — PostHog blocks headless), and (2) **no custom funnel events**, so nothing measures booking-started → checkout → confirmed. That funnel is the booking reporting Wix gave for free and is the genuine gap versus the old platform. **User's call: consider it, does NOT block launch.** **A full spec exists at `planning/spec/analytics-funnel-events.md`** — six funnel stages anchored to real code, plus the traps: `booking_confirmed` **cannot** be captured client-side (the Stripe webhook confirms bookings; `/book/confirmation` only reads and auto-refreshes, so a page event measures "came back", not "paid", and double-counts), **no PII in event properties** (children's data), and the drop-off reasons are already discrete branches in `BookingForm.tsx`. Detail in [[project_empowr_members_analytics]].
- **2026-08-19: merges were blocked by the auto-mode classifier** — both `gh pr merge` and a local `git merge` were refused; the merge only went through after the user left auto mode. Expect this on future deploys, see [[feedback_automode_blocks_production_writes]].

- **[SUPERSEDED 2026-08-27 — 8 offerings are now active and the site is public.]** 2026-08-19 (tidy-up): Skate Jam `active=true`, the other three `active=false`; test data purged; ONE real booking deliberately retained.** The backlog said "Skate Jam `active=true` is smoke-test residue, user to deactivate" — **that framing was wrong and nearly caused a silent mistake.** Skate Jam was the *only* active offering; Sk8 Skool Kidz, Sk8 Skool All Ages and SYNKRON8 were already `active=false` despite all four having occurrences seeded through October. So "deactivate Skate Jam" meant "empty the public catalogue". **The user confirmed the site is NOT publicly launched yet**, which made an empty catalogue correct at the time. **REVERSED later the same session** — Skate Jam is back on so the retained booking matches a visible session. **🔴 But "not launched" is not the same as "not reachable": the site IS publicly reachable and indexable.** `/`, `/sessions` and `/login` all 200 to an anonymous request with no password gate; **`/robots.txt` does not exist** (it serves the app's 404 HTML); and `/sessions` carries **no `noindex`** in either a meta tag or an `X-Robots-Tag` header. Nothing stops a search engine crawling and listing it. Netlify password protection is a **Pro** feature and this site is not on Pro (same blocker as [[project_netlify_function_region_latency]]), so the cheap mitigation is a real `robots.txt` + `noindex` — **NOT DONE, offered, awaiting the user's call.** Verified live afterwards: `/sessions` 200s with the "check back soon" empty state.
  - **Deleted** — everything under `localdev.test@empowrcic.org` (`b3169b5d`): 2 participants (Adult/Minor Tester), 2 bookings (both `cs_test_`, one with a fabricated `pi_test_...`), 2 waiver consents, the account row. Archived to JSON first (validated as parseable *before* the transaction), deletes in one `begin/commit`, child rows first.
  - **🔴 KEPT ON PURPOSE — do not delete without asking**: booking `ee8e2e4a` on `admin@pecuvate.com`, £7, `cs_live_` + real `pi_3U5ks1CpJGJ55gu5...`, participant Shaun Barnett, Skate Jam **2026-08-20 19:45**. User wants to review it first. **It is a LIVE charge — deleting the row does not refund the money**; that is a separate Stripe action.
  - **Also kept**: `tech@pecuvate.com` (`b649352a`), a real login with 0 participants / 0 bookings.
  - **`mem_credits` has two `NO ACTION` FKs into `mem_bookings`** (`source_booking_id`, `redeemed_booking_id`). Harmless here only because the table is empty — one credit row against a cancelled booking and the delete fails outright. Any future booking-purge job must clear credits first. Same shape as [[feedback_shared_db_fk_breaks_scheduled_purge]].
  - **Not touched, deliberately**: the `auth.users` row for the test account, Stripe customer `cus_V5dyanMpoqqZJY`, and the Waivers-app `waiver_response`/`person` records the consents referenced (owned by Empowr Waivers).
  - **⚠️ Toggling `active` in raw SQL does NOT update the public site.** `revalidateCatalogue()` drops two layers (the `catalogue` cache tag *and* the rendered pages) and only the admin API route calls it. It self-healed via the `revalidate = 300` backstop on `/sessions` + the 300s `unstable_cache` window — **use `/admin/offerings` instead, which invalidates immediately.** See [[feedback_db_write_bypasses_app_cache_invalidation]].
  - **`_config/registry/env-vars.md` follow-up from last session is DONE** — `ADMIN_EMAILS` now records the per-context split and the `env:get`-not-`env:list` rule. Fixed a stale neighbour too: `STRIPE_SECRET_KEY` said *"TEST until Step 9"*, but production has been live-mode since 08-18 (`.env.local` correctly stays `rk_test_`).

- **2026-08-19: `jasmine.barnett@empowrcic.org` granted ADMIN on PRODUCTION.** `ADMIN_EMAILS` production context is now `tech@pecuvate.com,admin@pecuvate.com,jasmine.barnett@empowrcic.org`. **This var is scoped PER CONTEXT and the others were left alone** — `deploy-preview`/`branch-deploy`/`dev` each hold only `tech@pecuvate.com`, so admin routes behave differently on previews by design. No code change: admin is an env-var allowlist (`lib/admin.ts` `isAdminEmail()`), enforced in the `(admin)` layout + `getAuthedAdmin()` on every `/api/admin/*` route; **there is no self-serve path to admin and RLS does NOT gate admin writes**, so this list is the only gate. **Read single keys with `netlify env:get KEY --context X` — never `env:list`/`getAllEnvVars` on this site**, which would dump the live Stripe secret and Supabase service-role key into the transcript. **Env changes need a redeploy AND a docs-only commit will not trigger one** (base dir is `src/`, root-only pushes cancel as "no content change") — use `netlify api createSiteBuild`. Jasmine needs no pre-created account; the gate matches on email, she just signs in at `/login`. **✅ CORRECTED 2026-08-30: she HAS signed in and verified** — user-confirmed, and she holds a `mem_accounts` row (0 participants, 0 bookings), which only the signup trigger creates. The earlier "NOT verified e2e" is superseded. `_config/registry/env-vars.md` still lists the old membership.

- **2026-08-19 (end): unified site header (PR #9 MERGED, live).** `/sessions` sits in the `(public)` group and always rendered `PublicHeader` — different links (no **Bookings** at all), no burger, wordmark hidden — so a signed-in member tapping "Sessions" landed on what felt like a different site. **PR #8 caused half of it** by upgrading member+admin headers and leaving public behind: **the third near-identical header again**, exactly the AdminHeader failure mode. `MemberHeader`/`PublicHeader` DELETED, replaced by one **`SiteHeader`** (Sessions/Bookings/Account) used by both layouts. **`AuthNavAction` resolves auth CLIENT-side on purpose** — server-side session reads call `cookies()`, which would de-opt `/sessions` + `/sessions/[slug]` from static and undo the caching work; verified both still `o (Static)` / `● (SSG)` **against the built HTML**, not the route table. **OPEN, deferred to a future session:** the pre-purchase `PolicyNotice` wording AND a contradiction found alongside it — `OfferingForm.tsx` labels the `standard` refund option **"Standard (48h refund/credit window)"** while the member copy for that same value says refunds are not offered (fallout from the 2026-07-21 T&Cs v1.1 rewrite that updated member copy but not the admin label). Resolve both together in Programme Policies v1.2.

- **2026-08-19 (later): member nav collapsed, scroll bug fixed, refund copy removed (PR #8 MERGED, live).** **🔴 Root cause of the "unnecessary scrolling": every route-group layout wrapped children in `min-h-screen` while the ROOT layout body is already `min-h-screen flex-col`** — the viewport height was counted twice, so the content div filled the screen alone and the footer stacked below it, making **every page scroll by at least the footer's 117px no matter how empty**. `/bookings` at 375px: 260px of content in a 700px viewport, 237px of overflow. Wrappers now use `flex-1`, background moved to `body`; overflow 237→0. My PR #5 consent spacer was the other half — it now only applies when the page already scrolls. Member header now collapses below `sm` like admin, via a **shared `CollapsibleNav`** (not a second copy — copying the header is what left AdminHeader behind). Removed remaining member-facing refund copy (ticket QR footer, invalid-ticket panel, confirmation email). **The pre-purchase `PolicyNotice` on `/sessions/[slug]` + `/book/[id]` is STILL THERE by prior explicit user decision — flagged, not silently reversed; awaiting a call.** Admin refund/credit controls untouched (real Stripe refunds, operational not copy).

- **2026-08-19: bookings notice removed, nav indicator + admin burger (PR #7 MERGED, live).** Removed the confirmed-booking "cancel/transfer — bookings are final, email enquiries@" block from `/bookings`. **The pre-purchase `PolicyNotice` on `/sessions/[slug]` and `/book/[id]` was KEPT deliberately** (user's choice when offered the distinction) — it is shown before payment and does pre-contract information work; the removed one was post-purchase repetition. Policy v1.2 will rewrite the stance anyway. Added an active-section indicator to all three headers (`NavLink`, `aria-current`, section-matched so `/sessions/[slug]` marks "Sessions"). **Burger nav on ADMIN ONLY** — I argued against burger-everywhere and the user agreed: admin has 4 items and was the header that actually overflowed at 320px, so collapsing it restored the "Members Admin" wordmark at all widths; member/public keep visible nav because hiding "Sessions" behind a tap would add friction to the booking funnel. **Tailwind v4 note:** `scale-*` compiles to the standalone `scale` property, NOT `transform` — `transition-transform` still covers it because v4 expands that to `transform, translate, scale, rotate` (verified in-browser, do not re-derive).

- **2026-08-18 (session 5): UX pass — PRs #4, #5, #6 ALL MERGED and live.** The app had **no `loading.tsx`/`error.tsx` anywhere**, which on the App Router means a server navigation shows the OLD page until the new one lands (a tap looks dead) and dynamic routes cannot be prefetched past a boundary that does not exist. Added boundaries for `(member)`, `(admin)`, `/ticket/[id]`, `/sessions/[slug]`. **`/sessions` filtering moved client-side**: 0 document requests per chip tap, route now `o (Static)` — session 3's "274ms is the floor" ceiling is GONE, because that floor was caused by reading `searchParams`. Filter rule now lives once in client-safe `lib/catalogue-filters.ts`. Consent banner was **covering the primary CTA on every page** (over Book buttons, over the `/book` participant list) — now 250px->121px with a measured-height spacer. Fixed a latent bug: `OfferingCard` value-imported from the `server-only` `lib/catalogue`. Built a `useLinkStatus` spinner then **removed it** — with a loading boundary present, `pending` never becomes true (0/40 samples). **[REGRESSION] Shipped a regression in PR #5, caught on the prod deploy, fixed in PR #6: `/sessions` prerendered its Suspense FALLBACK as the page's static HTML** (skeleton as rendered DOM, offerings only in the RSC payload) because `useSearchParams()` during render de-opts prerendering — **while the route still reported `o (Static)` throughout**. See [[feedback_static_route_can_prerender_skeleton]]: grep the built `.next/server/app/<route>.html`, never trust the route table alone. **framer-motion and the third-party ui-ux-pro-max skill both assessed and DECLINED for now** — motion would have decorated the dead air rather than closed it (and costs users zero bytes only while nothing imports it), and the skill is a greenfield design-system generator that would fight `brand-identity.md`, the authoritative system shared across 8 Empowr sites.

- **⚡ 2026-08-18 (session 4): mobile audit done — PR #4 MERGED 2026-08-18 as `984349f`; PR #3 (perf) MERGED as `108e6bb`. (Both were open when this was written.)** Ran the multi-viewport audit (320/360/375/414/768) across every page logged out / as member / as admin. **The real bug was `AdminHeader`** — it never got session 2's header fix (kept `px-6`, always-visible "Members Admin" wordmark, 4 nav items at `gap-5`), which forced the layout viewport to **356px and made every admin page scroll sideways at 320px**, wordmark colliding with the nav. That is the staff check-in surface used on a phone at the door. Fixed to match Public/Member headers; measured `innerWidth` 356→320, `canScroll` true→false. Also: session-detail `<li>` was `flex-wrap` so every Book button wrapped to its own line (page 2476→2052 CSS px at 375, -17%); `/sessions` age filter's `ml-auto` stranded the control right-shoved on its own mobile row (→ `sm:ml-auto`); tap targets 16-34px → 40-42px; all 16 `<main>`s used flat `px-6` vs headers' `px-4 sm:px-6`, so content sat inboard of the header. **CSS-only, no logic changes.** Public pages verified on the real deploy preview (no h-scroll at 320/375, rows uniform 68-73px); **the admin-header fix was verified locally at 320px on the identical built code, not on the preview** (admin routes need auth the preview can't easily grant). **Cleared, not bugs:** no overflow on any public/member page, no sub-12px text, the admin register table already had `overflow-x-auto` and only looked cut off because the page under it overflowed; remaining sub-44px hits are inline text links, remaining "clipped" items are native `<select>` closed states.

- **⚡ 2026-08-18 (session 3): performance pass — PR #3 MERGED 2026-08-18 as `108e6bb` (was open at time of writing).** The platform felt slow; the database was never the cause (all hot paths indexed, advisor clean, dataset is 4 offerings / 51 occurrences / 3 bookings). The catalogue was uncacheable by design (`force-dynamic` → `private,no-cache,no-store`, `Netlify Durable: fwd=bypass`), so every visit paid a function invocation plus a transatlantic round trip. Fixed with a cookie-free public client (`lib/supabase/public.ts` — `server.ts` reads `cookies()`, a dynamic API, which makes any page uncacheable), `unstable_cache` under one `catalogue` tag, and `revalidateCatalogue()` in all 12 catalogue-affecting admin write handlers. **Measured: `/sessions/[slug]` 586ms → 30ms p50.** `/sessions` only 344→274ms and **that is its ceiling** — it reads `searchParams` so it renders per request; going further means client-side filters. Auth round trips also cut (middleware early-return, React `cache()`, parallelised booking pages). **`getClaims()` DECLINED by the user** — ES256 keys would allow local JWT verification but `jwt_exp=3600` means a revoked session stays valid up to an hour; wrong trade for a payments platform, do not re-propose without raising that. **🔴 Gotcha: `export const revalidate` is INERT on a `[slug]` route without `generateStaticParams`** — builds and typechecks fine, silently stays dynamic; read the build legend. **Revalidation e2e-verified with a control window; the purge is async ~5s** and `ttl` is not a usable signal (absent on a miss) — use `"Next.js"; fwd=miss` + an `age` reset. Items 3-4 of the perf plan (streaming/`loading.tsx`, `next.config` tuning, asset cache headers) NOT done — deprioritised below mobile and bookings. `framer-motion` is unused but deliberately KEPT (costs users zero bytes since nothing imports it; removal is hygiene, not performance).
- **✅ 2026-08-18 (session 2): the live-mode Stripe smoke test has been RUN, not just unblocked.** Booked + paid £7 for Skate Jam through the real UI as `admin@pecuvate.com`: booking confirmed, ticket page + QR rendered, admin check-in marked attended and was idempotent on reload. **Skate Jam is left `active=true`** — deliberately not reverted here; user is deactivating it themselves via `/admin/offerings`, not yet confirmed done. Along the way: found + fixed a waiver bug (`emergency_contact_*` wrongly required for an adult self-signer, commit `b9f4b96`); found + fixed a real cross-app bug where the payment fired a genuine Empowr Heroes donation email (Heroes+Members share one live Stripe account; fixed in `empowr-heroes-nextjs`, see that project's memory.md); found (not yet fixed — Pro-plan-gated, deferred) that Netlify's `functions_region` is `us-east-2` against Supabase's `eu-west-2`; ran a Playwright mobile audit and fixed three real issues (**⚠️ NOT SUFFICIENT — as of session 3 the user reports the platform still does not feel responsive on mobile/small screens; a proper multi-viewport audit is outstanding, and re-running the same 375px overflow check is not enough, it already missed the header-wrap issue once**) (undersized "Book" touch targets, a 10px ticket label, and header/nav wrapping on narrow screens — commits `cf5377f`, `b648ba7`).
- **✅ 2026-08-18: PR #2 (tier-1 waiver decoupling + PassKit removal, bundled) merged to `main` and confirmed LIVE in production** — checked `members.empowrcic.org/ticket/<fake-id>` directly post-merge and got the app's own branded not-found page, not Netlify's generic 404, proving the deploy actually picked up the new routes rather than just trusting a green merge. Nothing left blocking launch except the two deliberate items below.
- **✅ 2026-08-17 (later session): PassKit removed entirely, replaced with an in-house QR ticket page.** No more Apple/Google Wallet integration — `/ticket/[bookingId]` is a new **unauthenticated** page (deliberate, user-confirmed: someone other than the account holder can show the ticket on their own phone) with a QR that encodes `/admin/checkin/[bookingId]`, scanned via native camera (not in-page — `netlify.toml` ships `Permissions-Policy: camera=()`). New admin check-in flow at that route (inherits the existing `ADMIN_EMAILS` gate, no new kiosk auth) is a read-only lookup + explicit "Mark attended" button, not an auto-mark-on-scan. `per_run` course bookings deliberately can't be marked attended (no per-week attendance concept in the schema) — surfaced with a note, not silently allowed. No DB migration — `passkit_pass_id`/`passkit_venue_id` left as harmless dead columns, drop in a later standalone migration. `lib/passkit.ts` deleted; `planning/passkit/CONTEXT.md` kept as historical record. **E2E-verified for real**: booked through the real UI to a real Stripe Checkout redirect, self-signed a `checkout.session.completed` webhook event, confirmed the booking flipped to `confirmed`, loaded the ticket in a fresh cookie-less browser context (proving the no-login claim), checked in via `/admin/checkin`, confirmed idempotent re-check, confirmed a cancelled test booking's ticket shows "no longer valid" instead of a ticket. Full detail: DEVLOG 2026-08-17 (later session entry).
- **✅ 2026-08-17: the waiver gate blocker is FULLY RESOLVED — PR #1 can merge.** `mem_waiver_consents` built (migration `20260817152231`) — decoupled gate, `revoked_at IS NULL AND (expires_at IS NULL OR expires_at > now())`, never touches `form_versions` or `waiver_responses` again. **Scope changed from the original 2026-08-10 design**: Waivers independently shipped a 3-year retention window on `waiver_responses`/`departure_consents` (2026-08-13/14, their own evidence-retention reasons) which made tier 2 (session-dated write at booking) redundant — dropped from scope. Tier 1 was still built anyway, not for retention but to stop the booking gate being hostage to a table/policy Members doesn't own, and to kill the `form_versions.active` landmine (any wording fix un-covering everyone instantly) that retention never touched. `checkWaivers()` is now two-tier: `mem_waiver_consents` primary, legacy email/name match against `waiver_responses` as fallback (still needed — it's what recognises someone who signed on the standalone `waiver.empowrcic.org` app before ever touching Members), with the fallback lazily backfilling a consent row. **Per-booking departure consent also built**: new `mem_participants.default_travel_method` (migration `20260817152232`) pre-fills a per-booking toggle; the 5 `confirm_*` affirmations always start unchecked and must be re-given every booking (2026-08-10 decision); writes into Waivers' own `departure_consents` table, not a new one. The old one-time `consent_unaccompanied_departure` write from `submitWaiver()` is retired (always `null` now). **E2E-verified against the real DB and a real Stripe Checkout session** — see DEVLOG 2026-08-17 for the full proof chain (queried `mem_waiver_consents`/`departure_consents` directly, didn't just trust `checkWaivers()`'s return value). PR #1's "only need to do this once" copy is no longer false — left as-is on `/waiver`. **Nothing schema/gate-related blocks launch anymore** — what's left (flipping offerings active, the live-mode Stripe smoke test) is launch sequencing, not a technical blocker. Full detail: DEVLOG 2026-08-17.
- **🔧 Reusable gotcha from this build**: on this machine, `Get-Content -Raw` in PowerShell does NOT return a plain string when piped into `ConvertTo-Json` — it carries pipeline metadata that inflated a 1870-byte SQL file into a 3.8MB JSON payload (413 from the Supabase Management API). Fix: `[System.IO.File]::ReadAllText($path)`. Relevant any time raw SQL is sent via the Management API curl/PowerShell path (see [[reference_supabase_management_api_sql]]).
- **📝 Applying a migration via the raw Management API `/database/query` endpoint does NOT register it in `supabase_migrations.schema_migrations`** — `dump-ledger.mjs` won't pick it up until you also `INSERT` a row into that table by hand (version/name/statements, statements = one array element with the full SQL text). Discovered because `dump-ledger.mjs` reported "0 written" right after a migration that had visibly applied to the live schema.
- **SUPERSEDED by the above — kept for history:** 2026-08-10: the retention half of the blocker below is FIXED at the database. Migration `20260810114838` applied: `delete-old-waivers` keys on `session_date`, departure consents get parity, `NOT EXISTS` replaces `NOT IN`, 13-month backstop added. **Also flipped `mem_participants.person_id` from `ON DELETE NO ACTION` to `SET NULL`** — the job ends with a mass `DELETE FROM people`, and pg_cron runs it as one transaction, so the first participant PR #1 linked would have aborted the whole job and **silently stopped all retention**. That was one deploy away from firing. Migration lives in `empowr-cic-workspace`, not here.
- **🛑 PR #1 is STILL ON HOLD — do not merge.** Its "you only need to do this once" copy stays false until tier 1 exists. The remaining work is entirely app-side: tier 1 table, tier 2 session-dated write at booking, per-booking departure consent, and repointing `checkWaivers()`.
- **📐 Tier 1 is designed, not built — don't redesign it.** `mem_waiver_consents`, one row per participant (adding a child tops up rather than forcing a re-sign). **Load-bearing decision: the gate checks `revoked_at IS NULL AND (expires_at IS NULL OR expires_at > now())`, NOT "consent against the active form version"** — that defuses the `form_versions.active` landmine where a typo fix would un-cover every member at once. `form_version_id` kept for audit only. **Must ship explicit `GRANT`s** or every PostgREST call fails `42501` on this project.
- **🧭 Departure consent is PER-BOOKING, not standing** (user decision 2026-08-10). A standing "my child may leave alone" consent is not equivalent in risk to a standing risk waiver — it is a judgement a parent may make differently on a dark December evening, and it carries a specific `travel_method`. Asked at each booking, pre-filled from an updatable default in the member profile.
- **🏗 Build paused 2026-08-10 for a shared-database architecture review, then cleared to proceed.** See `_config/guides/supabase-structure-blueprint.md` (DRAFT). Verdict: nothing blocks tier 1 — it is `mem_`-prefixed, touches no cron job and no `auth.users` trigger, and its only cross-app edge is to `form_versions` (append-only, no purge job).
- **🛑 2026-08-09 (retention half now RESOLVED above): the launch blocker is the waiver retention conflict — PR #1 is ON HOLD, do not merge.** `delete-old-waivers` (pg_cron, daily 03:00) deletes `waiver_responses` older than 24h, while `checkWaivers()` requires one to exist. So a member who signs today is blocked from booking tomorrow: **"sign once" is impossible against that table**, and the `/waiver` page built on `feat/in-app-waiver` promises exactly that in copy. A second defect in the same branch: it writes `session_date` = the *signing* date, but the Waivers staff check-in portal filters `session_date = today`, so a member signing ahead is invisible at check-in. **Agreed design (not built)**: two tiers — account-level standing consent in a new Members-owned `mem_` table immune to the purge, plus a session-specific step at booking writing a properly-dated `waiver_responses` row. **Next decision needed from the user before any build**: the purge keys on `submitted_at`, not `session_date`, so a per-visit row written at booking time dies before the session — either change the purge to `session_date < current_date` (one line, needs the Waivers owner) or have Members write the row on the session morning via a scheduled job (~double the work). About two-thirds of PR #1 is reusable. Full detail: [[project_empowr_waiver_gate_blocker]], DEVLOG 2026-08-09.
- **✅ 2026-08-09: waiver copy now mirrors the standalone form verbatim** (committed on `feat/in-app-waiver`). All three agreement cards carry the canonical wording, relationship is a select with the canonical eight options, `links.ts` gained `photographyConsent`, and **photo consent is now REQUIRED** to match. Also removed the emergency-contact backfill onto `mem_participants` — the standalone form promises "contact numbers are not stored on file". Separately flagged: mandatory photo consent is a shaky "freely given" basis under UK GDPR, inherited deliberately.
- **✅ 2026-08-05: catalogue seeded** — 4 venues (all with real `passkit_venue_id`), 4 offerings, 51 occurrences to 2026-11-01, capacity 25 set on `mem_venues.default_capacity` (so raising it later is 4 rows, not 51). **All four offerings are deliberately `active = false`** and must stay that way until the waiver architecture is settled. `PASSKIT_ENABLED` kill switch added, off unless explicitly `"true"` — DRAFT-mode passes carry a "Test Pass - Not for Commercial Use" stamp and expire in 48h.
- **📁 2026-08-06: migrations left this repo.** `src/supabase/migrations/` is gone. All 22 for the shared `empowr-cic` database now live in `Empowr CIC/supabase/migrations/`, generated from the migration ledger by `dump-ledger.mjs`. **Do not re-create `src/supabase/` here** — after applying any migration, re-run the dump and commit it there instead. Why: three projects share this database and per-app filing meant none could rebuild it (7 of 22 migrations had no file anywhere). Nothing was lost, and the 22 lines of schema rationale in `members_initial_schema.sql` were backfilled into the ledger first. Going forward, apply migrations **with their comments included** — the ledger stores only what was sent.
- **⚠️ CORRECTED 2026-08-05 — the bullet below is wrong on its central claim.** `mem_hold_bookings()` treats a NULL capacity as **unlimited**, not unbookable (`if v_capacity is not null and ...`, with an inline comment saying exactly that). Seeding was never blocked on capacity; the hold was self-imposed. Capacity is a safeguarding concern (overselling a room, coach-to-child ratios), not a technical gate. Catalogue was seeded 2026-08-05 with an interim capacity of 25.
- **🔍 Narrowed 2026-07-30 (SUPERSEDED, see above): Q6 (real-timetable seeding) is not a data-from-scratch problem — it's a capacity-numbers problem.** User asked whether the Empowr CIC KB (`entities/sessions.md`, as_of 2026-07-22, plus the older `sources/operations/sessions-booking.md`) could substitute for a fresh ask to Jasmine. It largely can: both pages have real venue/day/time/price/duration for every recurring session and dated 2026 course ranges. Confirmed with the user: the schedule has been stable 3+ years (so the KB's own "times/dates change" caveat is low-risk here), and **all sessions — including courses — are migrating fully from Wix to Members** (so courses aren't a separate ownership question). **What neither KB page has, at all: capacity/attendee limits per session.** `mem_hold_bookings()` needs a per-occurrence capacity to lock against — nothing can be seeded as a real bookable slot without it, and it's a genuine operational/safeguarding fact (room size, coach ratios, under-18 ratios) to get from Jasmine/Shaun, not to infer. **Next session**: once capacity numbers land, draft the full seed list (venues → offerings → recurring occurrences) from the KB data for a quick confirm pass instead of building from scratch — unblocks seeding → the live-mode Stripe smoke test → public launch.
- **✅ Done 2026-07-30:** instrumented with PostHog analytics (Variant B: cookieless `on_reject` + consent banner, same pattern as EELA) ahead of public launch — `PostHogProvider.tsx` + `CookieConsentBanner.tsx`, `analytics_sites` Supabase row created. No CSP change made deliberately (none exists yet; site runs Stripe + PassKit, first-time CSP needs its own audit). Not yet verified against real PostHog Live Events — zero real traffic pre-launch. A separate fleet-wide fix landed in `PostHogProvider.tsx` the same day (commit `1725d80`): `capture_pageview` was `true`, which disables posthog-js client-side route-change capture entirely. **This mattered more here than on any other site** — the booking and membership flows are almost entirely client-side navigation, so essentially the whole funnel would have been invisible from day one of real traffic. Nothing was lost (zero traffic so far), but it would have silently wasted the launch. Now `'history_change'`, verified in the deployed bundle. Full detail: [[project_empowr_members_analytics]], DEVLOG 2026-07-30.
- **✅ Resolved 2026-07-29 (spec risk #5, was a Phase 1 launch gate):** legal policy links wired into the site for the first time — previously zero legal links existed anywhere in the app. No new Sanity content needed: the org privacy policy (v1.2, updated 2026-07-28, `platform: "org"`) already covers programme-booking DOB/health data under "Programme Bookings" since Members is legally the same entity as the rest of Empowr CIC. Added the same `/legal/:slug` → `legalhub.pecuvate.com/share/empowr/org/:slug` Netlify proxy Main Site uses, `lib/links.ts` gained `privacyPolicy`/`termsAndConditions`/`riskWaiver`, new `Footer.tsx` mounted once in root `layout.tsx` (covers all route groups). Clean build verified; proxy target confirmed live (200). Full detail: DEVLOG 2026-07-29.
- **🔄 BEING REVERSED — decision 2026-08-18 (session 3): self-serve cancellation/transfer/reschedule is coming BACK, after the policies are amended to v1.2.** The removal below was correct against Terms & Conditions v1.1 / Programme Policies v1.1 ("All bookings are final"), both still v1.1 as of 2026-08-18. The user's reasoning for reversing is not a contradiction: the original no-cancel decision was driven by **admin workload** — handling requests by email would be unmanageable — and self-service *eliminates* that workload, so the original rationale now argues for the change. User explicitly rejected a request-and-approve middle path in favour of true self-serve. **Sequence: amend the 2 Sanity policies → update the KB → re-run `/sync-kb` (or the CRM chat widget will quote the OLD policy to real customers) → then code.** Zero DB migrations needed — `mem_booking_status` already has `cancelled/credited/refunded`, `mem_credits` exists, `mem_offerings.transferable` exists with an admin toggle already rendered (0 of 4 offerings currently flagged). Full blast radius in [[project_empowr_policies]].
- **✅ Resolved 2026-07-21 (was flagged 2026-07-20) — SUPERSEDED by the bullet above:** self-serve cancellation removed entirely — `lib/cancellation.ts`, the `POST /api/bookings/[id]/cancel` route, and the member-cancellation email are deleted; `BookingsList`/`PolicyNotice`/the confirmation email now all state bookings are final by default, discretionary exceptions via enquiries@empowrcic.org only. The only remaining refund/credit path is the admin occurrence-cancel route (Step 8) — already an explicit human decision, so it needed no functional change, just a stale-comment fix. Verified via clean `next build`; not verified live in-browser (dev DB has no seeded catalogue). Full detail: DEVLOG 2026-07-21.
- **🛑 SUPERSEDED 2026-08-17 — PassKit removed entirely, see Current Status above. Kept below for history only, do not build against this.** PassKit Track A: COMPLETE (2026-07-21) — built, e2e-proven live, and deployed. Full details, all real object IDs, and every REST-vs-gRPC gotcha discovered are in `planning/passkit/CONTEXT.md` — **read that before touching PassKit code again, do not rediscover any of it**. Summary: JWT auth works (claim `uid`, header `Bearer`, 60s-backdated `iat`); `lib/passkit.ts` has `createPassKitVenue`/`issueSessionPass`/`voidPass`/`passInstallUrl`, all never-throw. `mem_bookings.passkit_pass_id` + `mem_venues.passkit_venue_id` columns added; venue creation wired into `POST /api/admin/venues`. Stripe webhook issues one pass per booking row on first-confirm (`issuePassesForSession` in `lib/notifications.ts`; per-run bookings span the whole run via min/max of the run's occurrence timestamps); confirmation email renders an "Add to Wallet" button per participant with an issued pass; admin occurrence-cancel voids the pass after a confirmed booking's refund/credit succeeds. **Step A8 live e2e proof passed 2026-07-21**: self-signed a real Stripe webhook event against the running dev server, confirmed a real PassKit ticket was issued and persisted; read the real confirmation email via Gmail MCP and confirmed the wallet link; authenticated a real admin session via a temporary `ADMIN_EMAILS` addition + `generateLink`/`token_hash` (reusable pattern for future admin-route e2e proofs without needing an admin inbox) and confirmed the real admin-cancel route voided the pass (ticket 404s after). All seeded rows + test users cleaned up, zero leftovers. **Deployed**: commit `6d8f6b5` pushed to `main` 2026-07-21, PassKit env vars already confirmed live on Netlify beforehand. Key facts: pass install URL is `https://pub1.pskt.io/{ticketId}` (client-constructed, behind `passInstallUrl()`); `DeleteTicket` is a genuine hard delete; delete endpoints use different body field names per resource (`id` for venues, `ticketId` for tickets); **new fact from A8**: `DELETE /eventTickets/venue` refuses once a ticket's been issued against it (auto-created Event holds a reference, no Event-delete endpoint exists) — one test venue left as a harmless named orphan. **Blocked**: Apple Wallet passes need a Pass Type ID + cert from an Apple Developer Program membership — in progress, not ready yet; Google Wallet unaffected, not yet installed on a real phone (worth doing post-deploy, template rendering can't be asserted from code). Track B (membership pass) still blocked on Phase 2 — do not build.
- `ADMIN_EMAILS` now has two admins: `tech@pecuvate.com` and `admin@pecuvate.com` (added 2026-07-21, same three-leg local/vault/Netlify update path as the PassKit keys).
- **Live-mode Stripe smoke test: confirmed blocked 2026-07-29**, not just "not yet run" — checked production Supabase directly, catalogue is completely empty (0 `mem_venues`/`mem_offerings`/`mem_occurrences`/`mem_course_runs`). Same Q6/Jasmine real-timetable gap as Step 3. User decision: defer the smoke test until real seeding happens rather than build a throwaway test listing on the live site. **Also found a leftover `mem_accounts` row** (id `83602522-1602-421b-b034-defeeb448cca`, created 2026-07-21) from the PassKit Track A8 e2e session — that DEVLOG entry claimed "zero leftover rows verified" but this one row survived. User decision 2026-07-29: leave it as-is for now, don't delete.
- **[SUPERSEDED 2026-08-27 — the site IS now publicly launched.]** Phase: **Phase 1 built and deployed** (confirmed 2026-07-14; correcting earlier phrasing that conflated "Stripe in live mode" with "launched"). Steps 2, 3-pages, 4, 5, 6, 7, 8 DONE. **Step 9 (Verify + go live) in progress as of 2026-07-12**: full e2e pass (signup->book->cancel->credit) 6/6 PASS via delegated agent, zero app bugs; `/pre-deploy-security` 0 FAILs; `/netlify-supabase-check` clean after removing a dead `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` stub. **Stripe credentials switched to LIVE in production** (2026-07-12) — `MEMBERS_STRIPE_SECRET_KEY_LIVE`/`MEMBERS_STRIPE_WEBHOOK_SECRET_LIVE` vaulted, `sync-to-netlify.ps1` repointed, Netlify `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` set to live values scoped to the `production` context only (previews/branch-deploys still on TEST), production rebuilt + confirmed serving. This means real bookings *would* charge real cards if anyone booked — but the site hasn't been announced/launched yet, so **no real member bookings or payments have occurred**. **Remaining before launch**: ~~privacy policy live via LegalHub~~ — resolved 2026-07-29, see Current Status above; a live-mode smoke test (blocked on empty catalogue as of 2026-07-29, not just "not yet run"), registries update, phase-1 doc compression. Step 3 real-timetable *seeding* still blocked on Q6, but narrowed 2026-07-30 to specifically capacity numbers (see Current Status above — schedule/venue/price data is already derivable from the KB) — the Step 8 admin area is where that seeding will happen once unblocked; prod /sessions shows "timetable being finalised" until then. Business-rule values live as constants in `src/lib/business-rules.ts`. shadcn still deferred — brand-token primitives in `components/ui/form.tsx`. Deployed at https://members.empowrcic.org (Netlify site `76f903e4-3795-406a-9478-34be6b0ed015`) but pre-launch. **Deploys via GitHub push → CI**; `publish = ".next"` — CI resolves it relative to base (never CLI-deploy). Env-var updates to an *already-live* Netlify site are a separate path from that — see [[feedback_netlify_env_update_api]].
- Step 8 shape (for Step 9 to reuse): `(admin)/admin/*` allowlist-gated via `lib/admin.ts` (`ADMIN_EMAILS`, checked in the layout + every `/api/admin/*` route — middleware only session-guards `/admin`). Full CRUD for venues/offerings/occurrences/course-runs, register view, and cancel-occurrence (bulk refund/credit reusing Step 7's primitives in a loop, folded per-account into one notice email, deliberately skipping the member-facing 48h/non-refundable policy gate since Empowr-initiated cancellation isn't subject to it). e2e-verified incl. a folded multi-child email and FK-blocked venue delete. **Real bug found+fixed**: never import from `lib/catalogue.ts` (has `import "server-only"`) into a client component — it silently breaks the whole bundle, not just that page; offering-type constants now live in client-safe `lib/offering-types.ts`.
- Step 7 shape (for Step 8/9 to reuse): `lib/cancellation.ts` = pure `evaluateCancellationPolicy(refundPolicy, startsAt)`, shared by the page's render-time estimate and the API's authoritative check. `POST /api/bookings/[id]/cancel` = atomic claim (`confirmed`→`refunded`/`credited` guarded by `.eq("status","confirmed")`) *before* the Stripe partial-refund call or `mem_credits` insert, rolled back to `confirmed` on failure. Refund is partial against the booking's own `stripe_payment_intent_id` (safe even though a multi-child booking shares one PI per Checkout session — each booking only ever refunds its own `price_paid_pence`).
- Step 6 shape (for Steps 7/8 to reuse): emails via Resend, sender `Empowr CIC <members@empowrcic.org>` (Resend-verified; same as auth SMTP), reply-to general@empowrcic.org. `lib/email.ts` = client singleton + `sendEmail()` that **never throws** + brand-inlined primitives (`emailLayout`/`panel`/`detailRow`/`ctaButton`/`esc`). `lib/emails/*` = 3 **pure** builders (confirmation, cancellation refund|credit, occurrence-cancelled) returning `{subject,html}`. `lib/notifications.ts` = orchestrators (recipient via account→`auth.admin.getUserById`): `sendBookingConfirmationForSession` (wired into the Stripe webhook, first-confirm only, failure-swallowed), and `sendBookingCancellationEmail`/`sendOccurrenceCancelledEmail` for Steps 7/8 to call. `membersUrl()` in links.ts for absolute email URLs. **Deploy-time TODO: confirm `RESEND_API_KEY` is actually pushed to the live Netlify site** — it IS in `sync-to-netlify.ps1 $siteVarMap` for the Members site (76f903e4…, line 133) but Step 5 only pushed the two Stripe keys; the webhook's Resend call fails-soft (logs, no email) if the key is missing in prod, so run the sync before relying on confirmation emails going live.
- Step 5 shape (for Steps 6/7 to reuse): `POST /api/bookings` holds then creates a **card-only** Checkout session (`getStripe()` in `lib/stripe.ts`; one line item per participant at the snapshotted `price_paid_pence`), links `stripe_checkout_session_id` onto the holds and **extends `expires_at` past the session's 31-min expiry by `HOLD_GRACE_MINUTES` (10)** so a late payment beats the pg_cron sweep; session-creation failure releases the holds inline; returns `{ checkout_url }`, `BookingForm` redirects. Webhook `POST /api/webhooks/stripe` (async-verified, idempotent): `completed`(paid)→`confirmed` + PI persisted + `expires_at` cleared (paid-for-released-holds logged loudly for manual refund — no auto-refund until Step 7); `expired`→release; bad/missing sig→400. `/book/confirmation?session_id=` reads own bookings by session id + `AutoRefresh` polls ~20s. Stripe **customer created+persisted per account on first pay** (`mem_accounts.stripe_customer_id`) — Phase 2 Billing reuses it.
- Step 4 shape (for Step 7 to reuse): holds via `mem_hold_bookings()` RPC (row-locked, service-role only; inline expired-release; price snapshotted into `price_paid_pence`); expired pendings → `cancelled` via pg_cron `members-release-expired-pendings` (every minute); waiver gate in `lib/waivers.ts` (active form version + `skater_names` name match, persists `person_id`, fails closed, blocks with 409 + waiver link). Gotcha: service_role can't DELETE from `people`/`waiver_responses` — waiver-row test cleanup goes via SQL.
- **⚠️ CORRECTED 2026-08-27 — the endpoint named below is the TEST one and is now DISABLED.** Production is the **LIVE** endpoint `we_1TsQVOCpJGJ55gu5bs8mlJDd`. `we_1TraTS…` pointed at the production URL, which holds the live endpoint's secret, so its test events could never verify — dead since live-mode go-live on 2026-08-18. Historical note follows: Stripe env wiring (2026-07-10): prod webhook endpoint `we_1TraTSCpJGJ55gu5LdKiZb3e` (TEST) → members.empowrcic.org/api/webhooks/stripe; vault `MEMBERS_STRIPE_WEBHOOK_SECRET` = that endpoint's secret; `.env.local` `STRIPE_WEBHOOK_SECRET` = a LOCAL self-signed secret (not the prod one). `sync-to-netlify.ps1 $siteVarMap` Members entry has both Stripe rows; push per-key (bulk POST 422s when 5 vars already exist). **Stripe CLI login key expired** — `stripe listen` 401s; local webhook e2e done by self-signing events with `generateTestHeaderString`; `rk_test` restricted key can't run `listen` either.
- Step 2 shape (for later steps to reuse): auth = password + magic-link tabs (`shouldCreateUser: false` on OTP; no password-reset flow — magic link is recovery); middleware Pattern 1 guards /account /bookings /book /membership /admin; all writes via zod-parsed API routes with service client scoped to `getAuthedAccount()` (lib/auth.ts); e2e pattern = admin-createUser (email_confirm) + Playwright, cleanup deletes user (cascades).
- 2026-07-08 kickoff session: e2e signup test PASSED end-to-end (signup 200 → Resend SMTP email delivered to inbox in 2s from members@empowrcic.org → confirm link verifies user → mem_accounts trigger fires; test users deleted after). Found + fixed a Phase 0 defect: `mem_` tables had **no table-level grants** (project default ACL is hardened — new tables get no DML for API roles); `members_table_grants` migration applied and verified (service_role full DML, anon catalogue-only, authenticated + own-row tables). src/.env.local written; project registered in the vault pipeline (MEMBERS_* keys seeded; consolidate/pull/sync scripts all carry a members entry). Also fixed sync-to-netlify.ps1 (broken vault read → get_all_secrets RPC).
- Complete: plan, MWP scaffold, ADRs, GitHub remote + registries, execution plans for phases 0–4, Phase 0 (brand, schema, auth, Netlify + domain)
- Leaked-password protection Pro-plan-gated (accepted). **Test email — UPDATED 2026-08-29 by the user: use `tech@pecuvate.com` for ALL projects** (`tech+<tag>@pecuvate.com` when a fresh account is needed — the bare address already has a Members account, `b649352a`). `teams+` addresses **do** deliver; the Gmail connector here just cannot see that mailbox, which is a tooling limit, not a delivery failure — see [[feedback_absence_of_evidence_in_one_tool]].

## Key Decisions

- Stripe test/live key handling (agreed 2026-07-09): vault holds TEST values under `MEMBERS_STRIPE_SECRET_KEY`/`MEMBERS_STRIPE_PUBLISHABLE_KEY` (local dev pulls these); at Step 9 go-live, add `_LIVE`-suffixed vault entries via the same .env.shared intake and point the members entry in sync-to-netlify.ps1 `$siteVarMap` at them — app code reads the unsuffixed env var everywhere; webhook secret gets the same twin treatment

- Domain: members.empowrcic.org (dashboard.empowrcic.org reserved for Empowr Dashboard)
- Database: existing `empowr-cic` Supabase project — waiver tables live there, so the waiver gate is a table join
- Payments: Stripe Checkout (one-off) + Billing (memberships); account = the shared **Empowr CIC Stripe account** (org account, currently only used by Heroes — CONFIRMED 2026-07-08). Members gets its own API keys (`MEMBERS_STRIPE_*`) in that account at Step 5; never reuse or rename Heroes' keys
- EELA stays discovery-only; this project is the transactional layer
- All new tables `mem_` prefixed; writes via service client only

## Preferences

- Make reasonable implementation decisions without asking; only block on genuine business questions (entitlements, Stripe account)

## Parked work waiting on this project

- `feat/my-account-nav` (Empowr Main Site) and `feat/members-account-notice` (Empowr EELA) — merge at Phase 4 cutover; set `membersUrl` in Main Site links.ts first

## Pre-Close Checklist

- [ ] Update Current Status to reflect what changed this session
- [ ] Record any new decisions in Key Decisions (ADR in planning/decisions/ if significant)
- [ ] Note any outstanding work that was not completed
